# UE_GPIO token 优化试验

验收线预先固定：完整结束、token 统计完整，总 token < HAVEN 的 331,350，综合覆盖不低于原 RVProbe 的 99.14545454545454%。固定 deepseek-v4-flash-vision-exp、三轮预算、每 intent 最多四条 sequence、原 DUT/Stage-1、原始 LTL 回放验收；HAVEN 不改。模型自行选择 LTL，无历史答案注入。

| 版本 | token | 接受轮数 | 综合覆盖 | 时间（秒） |
| --- | ---: | ---: | ---: | ---: |
| 历史 HAVEN | 331,350 | 3 | 94.5159% | 782.32 |
| 历史 RVProbe | 1,224,376 | 3 | 99.1455% | 1,427.82 |
| 优化 v1 | 623,100 | 3 | 97.4545% | 1,148.45 |
| 优化 v2 | 528,792 | 2 | 99.3455% | 770.98 |
| 优化 v3（输出截断失败） | 159,503 | 0 | 54.2607%（基线） | 276.31 |
| 优化 v4 | 493,392 | 3 | 98.2774% | 1,136.93 |
| 优化 v5 | 405,052 | 3 | 98.5455% | 1,339.58 |
| 优化 v6（第 2 轮截断失败） | 181,040 | 1 | 90.1145% | 677.74 |
| v7（原样复测 v6 配置） | 278,490 | 3 | 99.3455% | 1,172.13 |
| v8（通用引用类型说明） | 221,390 | 3 | 98.7455% | 1,126.39 |
| v9（JSON 位置诊断） | 244,652 | 3 | 99.1455% | 1,162.78 |
| v10（同配置复测，首轮截断失败） | 140,026 | 0 | 54.2607%（基线） | 295.03 |
| v11（LTL-only＋裸端口） | 254,207 | 3 | 99.3364% | 1,023.39 |
| v12（取证顺序＋围栏解包＋无损反馈压缩） | 167,261 | 3 | 98.9273% | 1,703.95 |
| v13（内置 helper，模型只调用） | 168,394 | 3 | 99.3455% | 1,654.36 |

v1 降低成本但覆盖下降；v2 保住覆盖但仍高于 HAVEN，均未达标。v2 第三轮模型停止，不等同于已证明覆盖闭合；第三轮消耗包含在总数中。第二轮四个 intent 中一个不可满足，三个成功求解。历史对照为三设计并发，本次为单设计，因此运行时间不能直接作公平加速结论。

v2 使用无损列式覆盖表和无目标接线骨架。全部 529 条覆盖缺口可还原，约 60,086 字符缩为 19,701 字符，仍按需读取。实际模型仍请求小页并重复读取框架示例；第一轮末次请求携带 157,921 字符的历史推理，只有计数入档，不展示正文。

v3 的额外改动：覆盖工具自动决定页长，模型只传续页偏移；同时绑定已审查文档和 skill 的哈希，省略六份与核心说明重复的参考，新文档/修改文档仍可读取。没有修改推理档位、协议消息字段或模型预算。skill-creator 指导把通用语法集中于核心说明，不收录 DUT 操作数、验证目标或答案。407 项测试：391 通过、16 跳过。v3 在线结果以独立 comparison.json 为准，未提前宣称达标。

v3 已失败：最后一次请求输出 65,535 token，其中推理 64,104，正文被截断。工具请求只有四次，但不是成功的低成本结果。v4 在相同预算下增加纯符号谓词的完整三步 LTL 用法，说明模型表达关系、求解器搜索 witness，避免先手算具体激励。示例直接取自 skill，在真实 Nix/bubblewrap/scalac 下编译通过；无设计答案，也未调整模型推理档位。

v4 完整结束，无编译修复，但成本与覆盖均未达验收线。v5 保持模型推理设置不变，复用当前 run 的已读 RTL 证据（源码哈希、原文和偏移校验），不跨 run 复用答案。统一 RTL 工具上限为 32,768 字符/2,048 行，新覆盖数据仍按需读取。412 项回归：396 通过、16 跳过；测试覆盖首次无 RTL、跨轮精确范围、重叠合并、源码变化/伪造内容拒绝、超限显式分页、其他 run 隔离和完整小文件单次读取。

v5 仍未达标。第二轮继承 24,444 字符已读 RTL，仅一次工具读取新覆盖；第三轮额外读取 verbose coverage，再次放大历史推理输入。第一轮局部编译修复实测只删除一个多余右括号，额外 19,648 token，修复费用包含在总数内。

v6 向后续轮直接提供完整的小型结构化覆盖报告和本次已接受的 UT；已完整提供的内容不重复开放读取入口。超限仍可完整分页。首轮保持 spec/IO + 按需读取，所有模型调用/推理设置不变。skill 中新增具名中间序列写法，真实隔离编译通过；414 项回归：398 通过、16 跳过。

v6 结果：首轮只用两次请求、87,500 token，编译求解一次通过；第二轮无工具往返，但单次输出 65,536 token 全部为推理，正文长度为零，finish_reason=length，正常记录失败。没有自动续写、降档或提高输出上限。不能把 181,040 token 的失败当作低成本胜出。

六次 pilot 共计 2,390,879 token（包含此前的 v1；失败费用全部保留）。最低成本完整运行是 v5，但仍比 HAVEN 贵；保住原 RVProbe 覆盖线的完整结果是 v2，仍未满足成本验收线。尚未证明总体上或稳定地优于 HAVEN。

若继续研究输出截断，建议另开模型输出上限对照，并显式对齐两侧预算；不将推理档位/输出预算变化冒充纯框架优化。官方 API 说明 thinking 模式默认输出上限为 64K，可显式设置 max_tokens；参考 https://api-docs.deepseek.com/api/create-chat-completion/ 。本轮未修改此设置。

全部 trial 存于 `/var/storage/workspaces/clo91eaf/rvprobe-token-ab-ue-gpio-20260913-v{1,2,3,4,5,6}/comparison.json`；每次完成校验归档并释放对应 `/dev/shm` 目录。报告保留各版本结果，不自动反复抽样择优。

## v7：同配置复测，首次单次达到预设验收线

2026-09-13 新开一轮 UE_GPIO 复测，未修改模型、输出预算、推理设置、API、skill 或提示词；框架源码哈希、固定 Stage-1、模型和轮次/采样设置均与 v6 一致。新整理的 `docs/zaozi-ltl-api.md` 没有注入模型，因此不能把本次变化归因于 API 文档。未调用 HAVEN 模型，仍使用上述历史对照。

本次完整完成 3 轮，6 次模型请求，共 278,490 token，比历史 HAVEN 少 15.95%，比原 RVProbe 少 77.25%；综合覆盖率 99.3455%，达到预先固定的 99.1455% 覆盖下限。最终 line/cond/branch 均为 100%，toggle 为 97.3818%。所有请求的 token 统计完整，无截断或超时。

| 轮次 | 本轮 token（含修复） | 模型请求次数 | 新增 sequence | 累积综合覆盖率 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 79,117 | 2 | 12 | 89.2919% |
| 2 | 119,477 | 3 | 12 | 95.6370% |
| 3 | 79,896 | 1 | 16 | 99.3455% |

共 12 个 intent，前两轮各有一个 `infeasible`，其余 10 个各接受 4 条 sequence，共 40 条；原始 LTL 回放检查保留，没有放宽验收。

Token 分解：输入 135,111，输出 143,379；输出中推理为 134,133，其余输出为 9,246。推理计数已经包含于输出，不能再加一次。第二轮初次 UT 把辅助函数参数标成 `Bits` / `Bool`，实际参数却是 `Const[Bits]` / `Node[Bool]`，产生 38 条编译诊断。模型修复仅将六个辅助函数的参数声明改为 `Referable[Bits]` / `Referable[Bool]`，目标及操作数不变；这次修复的两次请求额外花费 38,886 token（占本次总数约 13.96%）。可作为下一次 skill 的通用类型说明优化依据，但本次运行中没有补提示或手改模型答案。

RVProbe Stage-2 起止时间为 11:52:05–12:11:37 UTC（UTC+8 为 19:52:05–20:11:37），耗时 1,172.13 秒；含准备与归档的整次任务为 1,191.98 秒。历史 HAVEN 为 782.32 秒，但并发条件不同，不据此宣称速度优势。

这是单次达标，不是稳定胜出：同配置 v6 曾截断失败，v7 成功体现出运行结果的波动。七次 trial 合计 2,669,369 token，所有失败费用仍保留，不能只报告 v7 而隐藏先前结果。

本次归档：`/var/storage/workspaces/clo91eaf/rvprobe-token-ab-ue-gpio-20260913-v7/`。其中 `comparison.json` 包含历史对照与验收结果，`token-details.json` 包含分请求计数，`trial-plan.json` 记录事前约束，`export_comparison.py` 可重新导出。本次设计目录已校验迁移，`/dev/shm` 中只保留约 96 KiB 的顶层记录及归档软链接。

## v8：类型说明补充，未达到覆盖率下限

按 skill-creator 的窄范围、通用示例原则，在 skill 中加入 `Referable[D]` 参数和谓词返回值说明，以及纯符号 `same` / `gated` 辅助函数。无 DUT 名称、目标、地址或历史答案。同步更新已审查 skill 哈希，保留重复参考的去重策略，其余运行框架未改。54 项回归全部通过，包含直接提取 skill 示例进行真实 Nix/bubblewrap 编译，以及把引用类型退回 datatype 后编译失败的反例；skill 格式校验通过。

v8 完整完成 3 轮、6 次模型请求，共 221,390 token（输入 114,919，输出 106,471，其中推理 96,916），综合覆盖率 98.7455%，未达到 99.1455% 下限。最终 line/branch 为 100%，cond 为 97.6%，toggle 为 97.3818%。三轮费用分别为 81,617 / 95,777 / 43,996 token；覆盖率分别为 81.7744% / 98.1636% / 98.7455%。

本次没有编译类型错误，但第一轮两次 JSON 根对象提前闭合，均报告 `Extra data`，第三次才修复成功；额外两次格式修复花费 19,589 token。没有截断或超时，所有请求费用完整。三轮各 4 个 intent 均求解成功，共新增 48 条 sequence，并保留原始 LTL 回放验收。不能从单次没有类型错误推断该类错误已被彻底消除。

Stage-2 时间为 12:24:10–12:42:56 UTC，1,126.39 秒。结果已归档到 `/var/storage/workspaces/clo91eaf/rvprobe-token-ab-ue-gpio-20260913-v8/` 并释放本次 scratch 设计目录。此次迭代额外要求零格式/类型修复；报告将原成本/覆盖率验收与更严格的零修复验收分开，v8 两者均未通过。

下一项已验证修复：保留 JSONDecodeError 的行号、列号、零基字符偏移和最多 80 字符上下文；`Extra data` 额外明确仅允许一个根对象。非法响应仍严格拒绝，不做自动补括号、抽取子对象或改写模型源码。86 项回归：75 通过、11 跳过（含先前单独执行通过的编译测试）；覆盖长文本、Unicode、EOF 和提前闭合，适用于后续 v9，不追溯改写 v8。

## v9：一次完整零修复达标，继续同配置复测

相对 v8，运行代码只变化 `experiments/sequence_framework.py` 的 JSON 错误位置诊断，skill、模型、预算、Stage-1、采样和回放均不变。本次完整完成 3 轮、4 次模型请求，244,652 token，覆盖率 99.1455%，无格式或类型修复、无截断或超时，费用计数完整，达到预设的成本/覆盖率及零修复验收线。没有实际触发 JSON 修复，因此不能把本次成功归因于新增诊断。

三轮费用为 96,488 / 72,291 / 75,873 token，覆盖率为 79.8929% / 92.7182% / 99.1455%，新增 sequence 为 8 / 16 / 16。首轮两项 intent 不可满足，其余 10 项各采样并接受 4 条，共 40 条。最终 line/branch 为 100%，cond 为 99.2%，toggle 为 97.3818%。输入 103,414、输出 141,238，其中推理 133,029、其他输出 8,209。零修复不保证比每次有修复的实验都便宜，模型输出长度仍有波动。

Stage-2 时间为 12:44:55–13:04:18 UTC，耗时 1,162.78 秒。本次归档到 `/var/storage/workspaces/clo91eaf/rvprobe-token-ab-ue-gpio-20260913-v9/`，scratch 设计目录已校验迁移。v10 原样复测该配置，检验连续两次达标，不注入 v9 的目标、源码或 witness；本轮迭代最多新增 v8/v9/v10 三次付费试验，所有结果均保留。

## v10：同配置仍发生输出截断，连续达标目标未实现

v10 与 v9 的框架哈希、固定 Stage-1、模型、轮次和采样参数均相同，仍从新实验开始，不继承 v9 的回答。首轮先完成两次按需读取相关的模型请求，随后生成请求输出 65,536 token，全部为推理，正文为零，`finish_reason=length`。没有 UT 可编译；这不是 JSON 解析或类型错误，改进诊断无法在没有正文的情况下修复它。

本次失败费用为 140,026 token（输入 73,006，输出 67,020，其中推理 66,780），3 次模型请求，计数完整。接受轮次为零，54.2607% 只是基线覆盖率，不能称为模型生成结果。Stage-2 时间为 13:05:27–13:10:22 UTC，共 295.03 秒。失败已归档到 `/var/storage/workspaces/clo91eaf/rvprobe-token-ab-ue-gpio-20260913-v10/`，本次 scratch 设计目录也已迁移释放。

本轮三次实验 v8/v9/v10 合计 606,068 token；全部十次 pilot 合计 3,275,437 token，失败费用均包含。v9 单次达到成本、覆盖率和零修复验收，但同配置 v10 截断，所以未实现连续两次达标，不能宣称框架“完美”或稳定优于 HAVEN。

此次落地的改动仅为通用引用类型 skill 说明、同步去重哈希、JSON 错误定位及对应回归测试。若继续优化，需要单独评估模型输出契约等结构性改动；仅调整诊断不会解决 64K 推理耗尽且正文为零的问题。尚未改为 body-only/LTL-only 响应，尚未提高输出上限或修改模型推理设置，也没有把失败响应自动续写为成功实验。

## v11：LTL-only＋裸端口，一次完整达标

本次使用 `runtime-ltl-v2`、`ltl-bare-ports-v2` 和 RAG v16，模型只输出原始 LTL，框架生成 UT 外壳和 IO 别名，Bool 时序连接由原生 API 重载提供。同步的 skill 只包含通用语法与符号示例，不提供 DUT 场景或历史答案。固定 Stage-1、模型、三轮预算、每 intent 最多四条 sequence 与 v10 一致；未重新调用 HAVEN，未改变推理设置或输出上限。多项接口改动一起评估，不是隔离变量的消融实验。

2026-09-13 15:33:12–15:50:16 UTC 完成 Stage-2，共 1,023.39 秒（17 分 03 秒）。三轮、七次请求，总计 254,207 token，费用统计完整；输入 172,521，输出 81,686，其中推理计数 78,024、其余输出 3,662。请求模型为 `deepseek-v4-flash-vision-exp`，供应商响应中的模型字段为 `deepseek-flash`，两者分别保留记录。

| 轮次 | token（含修复） | 请求数 | 新增 sequence | 累积综合覆盖率 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 63,873 | 2 | 16 | 80.7372% |
| 2 | 159,003 | 4 | 16 | 97.8455% |
| 3 | 31,331 | 1 | 16 | 99.3364% |

十二个 intent 均得到并接受四条 sequence，共 48 条，保留原始 LTL 回放验收。最终 line/cond/branch 均为 100%，toggle 为 97.3455%。没有输出截断、超时或编译错误。第二轮第一次回答带 Markdown 代码围栏，被响应契约拒绝；模型自行修复，额外两次请求花费 14,842 token，包含在总费用中。框架没有剥除围栏或手改回答。

总 token 比历史 HAVEN 少 23.28%，综合覆盖率也超过原 RVProbe 的 99.1455% 下限，因此本次成本/覆盖率验收通过。但存在一次格式修复，不能称为零修复。相对最近成功的 v9，总 token 高 3.91%，输出 token 虽下降，输入 token 反而增加；第二轮先花较长推理才请求追加证据，后续请求携带历史内容，加上格式修复，仍有结构性开销。不能从这一次实验认定新接口必然更省或稳定胜出。

本次单设计运行，历史 HAVEN 对照是三设计并发，时间只作记录，不作公平加速结论。十一轮 pilot 合计 3,529,644 token，所有失败费用保留。本次完成后已校验归档至 `/var/storage/workspaces/clo91eaf/rvprobe-token-ab-ue-gpio-20260913-v11/`；原 `/dev/shm` 设计目录改为归档链接，临时根只剩约 96 KiB 的顶层记录。

证据：`comparison.json`、`token-details.json`、`trial-plan.json`、批次 `summary.json` 及逐轮 `events.jsonl`。启动与导出脚本为 `/var/storage/workspaces/clo91eaf/run-token-ab-ue-gpio-20260913-v11.py`，具有同版本重复运行保护；本次只运行一次，没有失败后自动重抽。

## v12：费用下降，覆盖率未保住验收线

2026-09-14 新开一次 UE_GPIO 实验，使用 `runtime-ltl-v3`、`evidence-first-ltl-v3`。
改动为先取证再构造 LTL 的通用提示、完整单围栏确定性解包、包装错误限制修复工具、后续轮无损列式反馈与重复统计去除。
固定 Stage-1、模型 `deepseek-v4-flash-vision-exp`、三轮预算及每 intent 最多四条 sequence 与 v11 校验一致。
未改 HAVEN、推理档位、输出上限、采样及原始 LTL 回放要求，不注入旧目标或答案。

Stage-2 于 02:27:37–02:56:01 UTC 完成，耗时 1,703.95 秒（28 分 24 秒）。
三轮、四次模型请求，总 token 167,261，全部费用计数完整；输入 89,526，输出 77,735，
其中推理计数 74,371、其他输出 3,364。供应商响应模型字段为 `deepseek-flash`，与请求模型分别记档。

| 轮次 | token | 请求数 | 新增 sequence | 累积综合覆盖率 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 65,746 | 2 | 16 | 85.5148% |
| 2 | 46,806 | 1 | 16 | 93.6364% |
| 3 | 54,709 | 1 | 16 | 98.9273% |

十二个 intent 均接受四条 sequence，共 48 条；没有模型修复、编译失败、输出截断或超时。
第一轮的单 Scala 围栏通过确定性解包，原回答和块内 LTL 哈希/偏移完整保存，块内内容未改写；
后两轮直接输出裸 LTL。第二轮实际使用完整的 99 条列式覆盖缺口，没有追加 RTL 请求。

总 token 比 v11 少 86,946（34.20%），比历史 HAVEN 少 49.52%。相对 v11，输入减少 82,995，
推理输出减少 3,653，其他输出减少 298；95.46% 的总 token 差额来自输入。
请求数从七次降为四次，减少了工具往返和格式修复，但本次是组合改动的单样本，
不能将全部收益归因于某一项提示或编码变化，也不保证下一次仍保持相同费用。

最终 line 146/146、branch 96/96（均 100%）；cond 123/125（98.4%）；toggle 2676/2750（97.3091%）。
综合覆盖率比 v11 低 0.4091 个百分点：条件覆盖少两个 bin，toggle 少一个 bin。
虽然高于历史 HAVEN 的 94.5159%，但低于原 RVProbe 的 99.1455% 下限和 v11 的 99.3364%，
所以原成本/覆盖率验收以及“优于 v11 且保覆盖”验收均未通过，不能只用低 token 宣称整体优化达标。

本次耗时反而长于 v11，最后一轮回放期间观察到编译/仿真进程存在 I/O 等待；
这是运行观察，不足以定量归因全部时间差，更不能将其描述为模型推理变慢或公平的速度结论。
没有因结果未达标自动重抽。十二次 pilot 累计 3,696,905 token，包含全部失败费用。

产物已校验迁移到 `/var/storage/workspaces/clo91eaf/rvprobe-token-ab-ue-gpio-20260914-v12/`，
包含 `comparison.json`、`token-details.json`、`trial-plan.json` 和全部逐轮计数、源码、witness、覆盖结果。
`/dev/shm` 中本次设计目录已改为归档链接，临时根仅剩约 96 KiB 顶层记录。
启动/导出脚本为 `/var/storage/workspaces/clo91eaf/run-token-ab-ue-gpio-20260914-v12.py`。

## v13：模型调用内置 helper，成本基本持平、覆盖回升

2026-09-14 使用 `runtime-ltl-v4`、`predefined-ltl-helpers-v4`，单次运行 UE_GPIO。
框架预提供 `Ltl.is/isZero/isOnes`，skill 只说明语义和调用方式；模型不能生成函数定义。
这些 helper 只处理类型、位宽与常量比较，不含 DUT 地址、协议时序或历史答案。
固定 Stage-1、模型、三轮预算、每 intent 最多四条 sequence 与 v12 的记录校验一致。
保留 v12 的取证与反馈策略、模型推理设置、输出预算和原始 LTL 回放；未重跑或修改 HAVEN。

Stage-2 于 03:57:59.908–04:25:34.267 UTC 完成，耗时 1,654.36 秒（27 分 34 秒）。
三轮、四次模型请求，token 统计完整：输入 91,499，输出 76,895，总计 **168,394**；
输出中推理计数 72,938、其他输出 3,957。请求模型为 `deepseek-v4-flash-vision-exp`，
供应商响应模型字段为 `deepseek-flash`，分别留档。

| 轮次 | token | 请求数 | 新增 sequence | 累积综合覆盖率 | helper 调用数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 70,011 | 2 | 16 | 88.7327% | 32 |
| 2 | 41,707 | 1 | 16 | 95.7774% | 34 |
| 3 | 56,676 | 1 | 16 | 99.3455% | 40 |

十二个 intent 都首次编译、求解通过，共接受 48 条 sequence；没有模型修复、截断、超时或回放失败。
三份原始 `model.ltl` 共调用 helper 106 次（`is` 53、`isZero` 27、`isOnes` 26），
函数定义数为零。局部 val 和设计相关表达式仍由模型生成，框架没有改写其验证意图。

最终 line/cond/branch 均为 100%，toggle 为 97.3818%，综合覆盖率 **99.3455%**。
相对 v12，总 token 增加 1,133（0.68%），覆盖率增加 0.4182 个百分点，耗时减少 49.59 秒。
输入增加 1,973，推理输出减少 1,433，其他输出增加 593；本次不能声称 helper 进一步降低总 token。
相对历史 HAVEN，token 少 49.18%，覆盖率更高，达到原先的成本/覆盖率验收线；
但不满足“比 v12 更少 token 且保覆盖”的严格验收。单次观察不足以证明因果收益或稳定优越性，
历史 HAVEN 并发条件不同，时间不能用于公平加速结论。

运行过程中第一轮仿真编译/覆盖统计曾观察到 NFS/I/O 等待，未量化其对总耗时的贡献。
实验完成后已校验并迁移至 `/var/storage/workspaces/clo91eaf/rvprobe-token-ab-ue-gpio-20260914-v13/`；
原 `/dev/shm` 设计目录已替换为归档链接，临时根只剩约 96 KiB 的记录，没有自动重抽。
十三次 pilot 累计 **3,865,299 token**，包含全部失败费用。

证据：该目录的 `comparison.json`、`token-details.json`、`trial-plan.json`、批次 `summary.json`
及逐轮原始 LTL、请求与事件记录。一次性启动/导出脚本：
`/var/storage/workspaces/clo91eaf/run-token-ab-ue-gpio-20260914-v13.py`。
