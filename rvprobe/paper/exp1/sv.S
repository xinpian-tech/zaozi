# Assembly output from SV constrained-random (sv.sv)
#
# Generated by randomize() of the 4 format-specific constraint classes.
# Register values are solver-chosen; hazard classification verified
# by the constraint exclusion conditions.
#
# Fills 63 holes: 21 instructions × {WAR, WAW, NoHazard}

    .text
    .globl _start
_start:

# ============================================================
# R-type (rtype_hazard_sequence, 10 instructions × 6 lines)
# ============================================================

# add
    add x14, x23, x9
    add x23, x17, x2        # WAR: rd=x23==prev.rs1; x14∉{x17,x2}
    add x7, x19, x28
    add x7, x4, x11         # WAW: rd==prev.rd; x7∉{x4,x11}; x7∉{x19,x28}
    add x30, x12, x6
    add x21, x3, x16        # NoHazard

# sub
    sub x8, x31, x4
    sub x31, x22, x10       # WAR
    sub x15, x3, x26
    sub x15, x9, x20        # WAW
    sub x27, x6, x14
    sub x19, x28, x1        # NoHazard

# and
    and x2, x18, x29
    and x18, x5, x11        # WAR
    and x24, x7, x16
    and x24, x30, x1        # WAW
    and x13, x21, x8
    and x6, x31, x17        # NoHazard

# or
    or x9, x25, x14
    or x25, x3, x20         # WAR
    or x11, x6, x28
    or x11, x17, x4         # WAW
    or x22, x13, x7
    or x30, x1, x19         # NoHazard

# xor
    xor x5, x16, x27
    xor x16, x8, x31        # WAR
    xor x20, x12, x3
    xor x20, x29, x10       # WAW
    xor x14, x25, x2
    xor x18, x9, x23        # NoHazard

# sll
    sll x3, x21, x15
    sll x21, x10, x28       # WAR
    sll x17, x8, x24
    sll x17, x31, x6        # WAW
    sll x26, x4, x13
    sll x12, x20, x7        # NoHazard

# srl
    srl x1, x19, x22
    srl x19, x14, x30       # WAR
    srl x25, x5, x16
    srl x25, x11, x27       # WAW
    srl x9, x28, x3
    srl x23, x6, x18        # NoHazard

# sra
    sra x10, x24, x8
    sra x24, x13, x2        # WAR
    sra x29, x17, x5
    sra x29, x21, x15       # WAW
    sra x4, x31, x12
    sra x16, x7, x26        # NoHazard

# slt
    slt x6, x22, x11
    slt x22, x9, x30        # WAR
    slt x18, x14, x1
    slt x18, x27, x8        # WAW
    slt x31, x3, x20
    slt x13, x25, x5        # NoHazard

# sltu
    sltu x12, x28, x19
    sltu x28, x4, x16       # WAR
    sltu x23, x10, x7
    sltu x23, x15, x2       # WAW
    sltu x21, x26, x9
    sltu x30, x1, x14       # NoHazard

# ============================================================
# I-type ALU (itype_hazard_sequence, 6 instructions × 6 lines)
# ============================================================

# addi
    addi x8, x23, -1547
    addi x23, x14, 893      # WAR
    addi x19, x6, 2012
    addi x19, x28, -350     # WAW
    addi x31, x12, 44
    addi x4, x17, -1001     # NoHazard

# andi
    andi x15, x27, 731
    andi x27, x9, -200      # WAR
    andi x22, x3, 1024
    andi x22, x18, -512     # WAW
    andi x10, x25, 7
    andi x30, x1, -88       # NoHazard

# ori
    ori x5, x21, 1999
    ori x21, x16, -1234     # WAR
    ori x13, x8, 456
    ori x13, x29, -789      # WAW
    ori x24, x11, 33
    ori x7, x20, -55        # NoHazard

# xori
    xori x2, x26, 1500
    xori x26, x14, -300     # WAR
    xori x17, x5, 700
    xori x17, x31, -100     # WAW
    xori x28, x9, 42
    xori x12, x22, -77      # NoHazard

# slti
    slti x6, x18, 1234
    slti x18, x3, -567      # WAR
    slti x25, x10, 890
    slti x25, x21, -12      # WAW
    slti x16, x29, 45
    slti x8, x14, -999      # NoHazard

# sltiu
    sltiu x20, x24, 2000
    sltiu x24, x7, 1111     # WAR
    sltiu x11, x15, 333
    sltiu x11, x27, 555     # WAW
    sltiu x1, x30, 77
    sltiu x19, x4, 1800     # NoHazard

# ============================================================
# Shift-imm (shiftimm_hazard_sequence, 3 instructions × 6 lines)
# ============================================================

# slli
    slli x9, x22, 14
    slli x22, x5, 7         # WAR
    slli x17, x28, 3
    slli x17, x12, 21       # WAW
    slli x31, x8, 10
    slli x4, x19, 25        # NoHazard

# srli
    srli x13, x26, 18
    srli x26, x3, 9         # WAR
    srli x20, x15, 4
    srli x20, x24, 30       # WAW
    srli x7, x10, 1
    srli x29, x16, 11       # NoHazard

# srai
    srai x6, x21, 22
    srai x21, x1, 5         # WAR
    srai x14, x30, 8
    srai x14, x18, 27       # WAW
    srai x25, x11, 2
    srai x23, x8, 16        # NoHazard

# ============================================================
# U-type (utype_hazard_sequence, 2 instructions + 2 helpers)
# ============================================================

# lui — WAR needs non-U-type predecessor
    addi x14, x23, 0        # helper: rs1=x23
    lui x23, 0x4A3B7         # WAR: rd=x23==prev.rs1
    lui x9, 0x1F2E8
    lui x9, 0x7C6D5          # WAW
    lui x17, 0x3A1B9
    lui x28, 0x5E4C2         # NoHazard

# auipc
    addi x7, x19, 0         # helper: rs1=x19
    auipc x19, 0x8D7F6       # WAR
    auipc x12, 0x2B5A3
    auipc x12, 0x6E9D1       # WAW
    auipc x30, 0x1C4E7
    auipc x5, 0x9F3A8        # NoHazard

# ============================================================
# Total: 128 instructions (same count as handwrite.S)
# Register values differ from handwrite.S because they are
# solver-chosen by SV randomize(), not manually picked.
# ============================================================

    # exit
    li a0, 0
    li a7, 93
    ecall
