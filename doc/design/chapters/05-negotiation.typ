#import "../lib.typ": *

= 协商算法 <ch-negotiation>

协商是一个纯函数：输入设计规格，输出协商结果或一个错误。它是实现互连模型（@ch-interconnect）的算法：规格给出总线、成员、附着与落点，协商据此为每个成员结算接口、为每条总线算出地址图与可达关系，并装配出交给各物理互连的清单（@sec-manifest）。贯穿本章的两个词沿前几章的定义：附着把一个成员——确切说，它的模块侧*节点*，即成员在协议上的接入点——接到总线的一个具名节点上（@sec-attach）；为这次附着结算出的那条点对点链路，是协议意义上的一条*边*（@sec-protocol-object）。协商的全部计算发生在这些边上。本章把协商拆成一列顺序执行的遍，规定每一遍的输入、输出与失败模式，然后给出参数传播的精确语义与协议参数的计算规则。

== 遍的流水线 <sec-passes>

#图([协商的遍。每一遍是纯函数；箭头是唯一的数据流。校验拦截结构错误；中间四遍（两遍传播、边结算、协议参数）算出全部参数；清单装配把结果按总线打包；随后三遍规划端口、连线与层（@ch-hierarchy、#ref(<ch-verification>)详述），最终装配为协商结果。])[
  #syn-diagram(
    spacing: (5.5mm, 11mm),
    node((0, 0), [校验 \ #text(size: 8pt, fill: c-dim)[结构合法性]], name: <v>),
    node((1, 0), [下行传播 \ #text(size: 8pt, fill: c-dim)[@sec-propagation]], name: <pd>),
    node((2, 0), [上行传播], name: <pu>),
    node((3, 0), [边结算 \ #text(size: 8pt, fill: c-dim)[@sec-settle-pp]], name: <se>),
    node((4, 0), [协议参数], name: <pp>),
    node((0, 1), [清单装配 \ #text(size: 8pt, fill: c-dim)[@sec-manifest]], name: <ma>),
    node((1, 1), [打洞规划 \ #text(size: 8pt, fill: c-dim)[@sec-punch-planning]], name: <ph>),
    node((2, 1), [连线计划 \ #text(size: 8pt, fill: c-dim)[@ch-hierarchy]], name: <wp>),
    node((3, 1), [层合并 \ #text(size: 8pt, fill: c-dim)[@sec-layers]], name: <lm>),
    node((4.15, 1), [协商结果 \ `ResolvedDesign`], name: <r>, shape: fletcher.shapes.pill, fill: c-fill),
    edge(<v>, <pd>, "-|>"),
    edge(<pd>, <pu>, "-|>"),
    edge(<pu>, <se>, "-|>"),
    edge(<se>, <pp>, "-|>"),
    edge(<pp>, (4.75, 0), (4.75, 0.5), (-0.7, 0.5), (-0.7, 1), <ma>, "-|>"),
    edge(<ma>, <ph>, "-|>"),
    edge(<ph>, <wp>, "-|>"),
    edge(<wp>, <lm>, "-|>"),
    edge(<lm>, <r>, "-|>"),
  )
]

*校验*在一切计算之前拦截结构错误。前两项平实：附着两端协议一致——成员的模块侧节点与它附着的具名节点必须服从同一协议，类型系统已在构建期拦截绝大多数，此处是防御性复查；附着的多重性合法——一个模块侧节点恰好附着一次，一个具名节点至多承载一个成员（空置的节点合法，@sec-placement）。第三项最重要：*参数流无环*。附着决定了声明的流动：发起者的声明流入总线，总线的聚合流向响应者；而当一个成员把甲总线上的结算当作自己在乙总线上的声明——子总线对父正是这样，桥接成员亦然——参数便从甲流向乙。全部这类流动构成一张有向图，校验要求它无环，任何一项不成立都在此报出（@sec-error-semantics）。

为什么必须无环？下行与上行传播（@sec-propagation）各是一次单遍扫描，沿参数流的拓扑序推进，良定义依赖参数流不折返；参数流一旦成环——例如两条总线经两个转发成员互相依赖，各自的地址图都要引用对侧的并集——参数就成了需要迭代求解的不动点，单遍算法与"错误可定位"双双失守。这条约束并*不*排斥物理上带环的互连：一条边承载的是完整的双向物理链路（协议接口两个方向的字段都有，@sec-protocol-interface），环形、网状拓扑的回路属于物理互连内部，不出现在成员结构上；两个模块互为主从时，是两次方向相反的附着、两条独立的边——参数流依旧无环。真正被拒绝的只有*参数流自身的回路*，那是建模错误，不是拓扑需求。

*清单装配*把结算落成交接物。每条总线收拢自己的结果——每个成员的方向、结算接口、地址区域或标识空间、域归属，连同落点：协商不读它、不改它，原样抄入（@sec-placement）——组成成员表与可达表，即这条总线的清单（@sec-manifest）。这一遍没有任何求解：每个成员在它的具名节点上恰好一个端口，端口的数目、位置与次序在附着写下时已经确定，装配只是收拢成表。装配完成随即运行该总线 fabric 的能力校验——校验是清单与策略的纯函数（@sec-manifest），不必等到例化：承载不了的清单在协商期就报出（@sec-error-semantics）。

遍与遍之间是不可变数据。这带来两个直接可用的能力：任何一遍的输出都可以独立 dump 检查（@ch-tooling），任何一遍都可以在没有后续遍的情况下单元测试。

== 参数传播 <sec-propagation>

传播在总线的成员结构上做两遍，方向相反。下行承载发起者一侧的供给：每个发起成员在自己的边上给出 `Down`——它占用的标识空间、将发出的操作与流量；总线收齐后聚合，为每条响应者边产出一份 `Down`。上行承载响应者一侧的供给与要求：每个响应成员在自己的边上给出 `Up`——它服务的地址区域、它要求的保序、它接受的最大传输尺寸；总线聚合出整张地址图与可达关系，为每条发起者边产出一份 `Up`。两遍结束后，每条边两端各有一份就绪的参数——一份 `Down` 与一份 `Up`，其一来自成员自身的声明，另一来自总线的聚合——恰好是结算的一对入参（@sec-settle-pp）。

聚合发生在总线，也只发生在总线。一条总线收齐它全部成员的声明，算出整张地址图与可达关系，并为每个成员的边产出对侧参数：每个响应者边上的 `Down` 概括了所有能到达它的发起者——标识空间的并、操作种类的并；每个发起者边上的 `Up` 概括了它能到达的全部地址与那些响应者的公共约束。叶子成员只声明自己：处理器报告自己的并发标识数，存储控制器报告自己的地址区域与位宽能力，谁都不需要看见邻居——"看见全体"只是总线的职责。

子总线以同一姿态参与。作为父总线的一个成员，它在边上给出的声明来自内部聚合：内部响应成员地址区域的并集、一个结算出的接口——对父，它与叶子成员无从分辨（@sec-attach；对父呈现为发起者的子总线对称：它的 `Down` 聚合自内部发起成员）。反向亦然：父总线聚合出的参数经它注入内部，由清单里那个朝父的成员在内部总线上代言（@sec-manifest）。传播因此在总线树上递归，次序由参数流的拓扑序给出（@sec-passes）：面向父的声明依赖内部聚合，内部聚合依赖内部成员的声明。跨总线的位宽与时钟域适配不是一类成员，也不是传播的一环，而是落在*缝*上——子总线朝父的接口与父总线为该成员结算的接口各自定案，两侧不一致时，缝上的适配器在例化期弥合（@sec-manifest）。

#table(
  columns: (auto, 1fr, 1fr),
  table.header([参与者], [下行], [上行]),
  [发起者成员], [边上的 `Down` 就是自身声明：标识空间、操作种类、流量。], [无产出；接收总线聚合出的 `Up`。],
  [响应者成员], [无产出；接收总线聚合出的 `Down`。], [边上的 `Up` 就是自身声明：地址区域、保序要求、传输尺寸。],
  [总线], [聚合全体发起者的声明，为每条响应者边产出一份 `Down`。], [聚合全体响应者的声明——地址图与可达关系在此定案——为每条发起者边产出一份 `Up`。子总线把内部聚合为一份对父的声明，对父即一个普通成员。],
)

三条纪律使传播保持可判定与可测试：

+ *变换是纯函数。*成员的声明、总线的聚合与分发，服从与结算函数相同的三性质（@sec-protocol-object）：全函数、确定、不读环境。同一规格永远传播出同一组参数。
+ *聚合只在总线一处。*单个成员的变换不读相邻成员：叶子成员的声明只由它自身的用户参数给出，子总线对父的声明只由它内部的聚合给出。因此成员可以自由复用与并行求值，总线也无需理解任何成员的内部。
+ *计数即契约。*框架核对每一步的产出数量：总线必须为每条响应者边恰好产出一份 `Down`、每条发起者边恰好产出一份 `Up`。违约立即报错，指出总线与期望/实际数量（@sec-error-semantics），而不是任由错位的参数继续向下游传播。

#图([两遍传播。下行（蓝）：发起成员在各自边上给出声明（①），总线聚合后为每条响应者边产出一份（②）；上行（红，虚线）：响应成员给出声明（③），总线聚合出地址图，为每条发起者边产出一份（④）。子总线 `periph` 的 `Up` 是其内部聚合的并集，内部传播在它自己的总线上递归。两遍之后每条边集齐一对参数，进入结算。])[
  #syn-diagram(
    spacing: (26mm, 7mm),
    node((0, 0), [cpu \ #text(size: 8pt, fill: c-dim)[发起者]], name: <cpu>),
    node((0, 1.3), [dma \ #text(size: 8pt, fill: c-dim)[发起者]], name: <dma>),
    node((1, 0.65), [总线 `sys` \ #text(size: 8pt, fill: c-dim)[聚合·分发·地址图]], name: <bus>, stroke: (dash: "dashed")),
    node((2, 0), [dram \ #text(size: 8pt, fill: c-dim)[响应者]], name: <dram>),
    node((2, 1.3), [`periph` \ #text(size: 8pt, fill: c-dim)[子总线成员]], name: <per>),
    edge(<cpu>, <bus>, "-|>", stroke: c-down, label: text(fill: c-down, size: 8pt)[① `Down`], label-side: left),
    edge(<dma>, <bus>, "-|>", stroke: c-down, label: text(fill: c-down, size: 8pt)[① `Down`], label-side: right),
    edge(<bus>, <dram>, "-|>", stroke: c-down, label: text(fill: c-down, size: 8pt)[② 聚合 `Down`], label-side: left),
    edge(<bus>, <per>, "-|>", stroke: c-down, label: text(fill: c-down, size: 8pt)[② 聚合 `Down`], label-side: right),
    edge(<dram>, <bus>, "--|>", stroke: c-up, bend: 26deg, label: text(fill: c-up, size: 8pt)[③ `Up`]),
    edge(<per>, <bus>, "--|>", stroke: c-up, bend: -26deg, label: text(fill: c-up, size: 8pt)[③ `Up`（并集）]),
    edge(<bus>, <cpu>, "--|>", stroke: c-up, bend: 26deg, label: text(fill: c-up, size: 8pt)[④ 聚合 `Up`]),
    edge(<bus>, <dma>, "--|>", stroke: c-up, bend: -26deg, label: text(fill: c-up, size: 8pt)[④ 聚合 `Up`]),
  )
]

== 边结算与协议参数 <sec-settle-pp>

*边结算*对每个成员的边独立调用其协议的 `negotiate(down, up)`（@sec-protocol-object）：发起侧事实与响应侧事实在此合成边参数——这条链路的定论。结算失败即协商停止，错误附带该成员与附着行的书写位置。结算成功后，清单里该成员的接口由 `interfaceOf` 从边参数导出（@sec-protocol-interface），打洞规划与例化期的绑定校验（@sec-generator-contract）引用的也是同一份数据。逐边独立意味着结算天然可并行，也意味着一条边的失败报告不含任何别的边的噪声。

*协议参数*是协商结果进入生成器的唯一通道（@sec-two-layer-params）。每个生成器模块声明一个纯函数：

```scala
def computeProtocolParam(view: EdgeView): ProtocolParam
```

#term[边视图][`EdgeView`]是该*模块*的只读视图：模块内每个节点——该模块的每一重成员身份——映射到它结算出的那条边，每项含边参数、对端身份（总线与具名节点）与附着行位置，按节点的声明序排列。排序与条目身份都只来自声明序与落点（@sec-placement），因此协议参数的计算和其余一切同样确定。协议参数随后与用户参数合并为完整参数（@sec-two-layer-params），这份确定性直接支撑模块去重的正确性（@sec-dedup）。

#图([边视图。生成器模块 M 的两个节点各自附着在一条总线上，各有恰好一条已结算的边（绿）；框架把"节点 → 边"的映射收拢成只读视图，交给 `computeProtocolParam` 折算出协议参数。])[
  #syn-diagram(
    spacing: (17mm, 8mm),
    node((0.1, 0.35), [节点 a], name: <na>, shape: fletcher.shapes.circle),
    node((0.1, 1.15), [节点 b], name: <nb>, shape: fletcher.shapes.circle),
    node(enclose: (<na>, <nb>), stroke: c-hier, inset: 14pt, snap: false, name: <m>),
    node((0.1, -0.55), text(size: 8pt, fill: c-hier)[生成器模块 M], stroke: none),
    node((-1.35, 0.35), text(size: 7.5pt, fill: c-dim)[总线 `sys` 上的边], stroke: none),
    node((-1.35, 1.15), text(size: 7.5pt, fill: c-dim)[总线 `periph` 上的边], stroke: none),
    edge((-0.75, 0.35), <na>, "-|>", stroke: c-edge),
    edge((-0.75, 1.15), <nb>, "-|>", stroke: c-edge),
    node((1.55, 0.75), [`EdgeView`], name: <ev>, fill: c-fill),
    edge(<m>, <ev>, "..>"),
    node((3.05, 0.75), [协议参数], name: <pp>),
    edge(<ev>, <pp>, "-|>", label: text(size: 7.5pt)[`computeProtocolParam`], label-side: left),
  )
]

#决策([协议参数的依赖界限是本模块])[
  `computeProtocolParam` 的可见范围仅限*本模块*节点的边，不含其他模块，也不含全图。理由：一旦允许跨模块读取，A 的参数便可依赖 B 的边、B 又依赖 C——这张依赖网不出现在规格里，也不出现在任何声明中，只存在于各模块推导函数的调用链里。此时报错信息无法追溯参数值的来源：其推导链横跨任意多个模块的任意多层函数。经验上被这条界限排除的需求只有两类，且各有对应的表达方式：需要*全体接入者信息*的，应建模为总线的聚合（信息经传播抵达本模块的边，@sec-propagation）；需要*全局分配*的（如自动编址），属于构建期的规格前处理或协议库的图算法，不属于协商内核。
] <dec-pp-local>

#开放([显式多轮协商])[
  若未来出现既非总线聚合、又非规格前处理能表达的全局计算，候选方案是显式的多轮协商：第一轮的协商结果作为第二轮规格构造的输入。它保持每一轮内部的纯与界限，代价是使用者要自行保证轮间收敛。v1 不提供，等待真实用例。
] <open-multi-round>

== 错误语义 <sec-error-semantics>

协商的返回类型是 `Either[NegotiationError, ResolvedDesign]`：失败时返回*一个*错误，协商随即停止。这是刻意的选择：错误发生后，后续计算的输入已经不成立，继续执行所"收集"到的报错多为第一个错误的衍生物——衍生报错与真实成因混在一起，反而妨碍定位。遍间不存在带错继续的问题：失败遍之后的遍不再运行。

因此单个错误必须自带定位所需的*全部*上下文：类别（见下表）、主体（总线/成员/附着/模块的稳定标识）、全部相关书写位置（`SourceLocation`——重叠的双方、环的全程都在其中）、以及当时的参数快照。一个错误的报告是完整的；多个错误的清单不是目标。文本格式规范见@ch-tooling。

错误类别按报出它的遍组织，每类给出触发条件与报告的必含要素：

#table(
  columns: (auto, auto, 1fr, 1fr),
  table.header([类别], [报出的遍], [触发条件], [报告必含]),
  [*N1* 协议不一致], [校验], [附着两端协议不同：成员的模块侧节点与它附着的具名节点不服从同一协议。类型系统在构建期已拦截绝大多数，此处防御性复查。], [两端各自的协议名；成员与总线的标识；附着行位置。],
  [*N2* 参数流成环], [校验], [参数流存在回路，传播所需的拓扑序不存在。], [环上每一步的总线、成员与附着行位置——完整回路，不是片段。],
  [*N3* 地址重叠或不可达], [传播], [同一总线上两个响应成员的地址区域相交；或某发起成员要求到达的地址无成员服务、也无默认路由可走。], [重叠：两个成员、两段区域及其交集；不可达：发起成员与落空的地址；当时的地址图快照。],
  [*N4* 接口结算失败], [边结算], [某成员边上的 `negotiate(down, up)` 返回参数冲突。], [成员与总线的标识；两侧 `Down`、`Up` 快照；协议给出的冲突描述；附着行位置。],
  [*N5* 落点非法], [校验], [附着引用的节点名在该总线的 fabric 上不存在；坐标糖越出 fabric 拓扑；或同一具名节点被附着两次。], [引用的名字或坐标；该 fabric 实际暴露的具名节点全集；涉事附着行位置（重复占用时给出两处）。],
  [*N6* 能力校验失败], [清单装配], [fabric 承载不下装配好的清单：端口数、标识宽度或地址图形状超出实现上限（@sec-manifest）。], [清单中超限的那一项；被违反的那条限制及其上限值；总线与 fabric 策略。],
  [*N7* 协议参数数量违约], [传播、协议参数], [计数契约核对失败（@sec-propagation）：总线聚合或分发的产出份数与成员边的条数不符；或 `computeProtocolParam` 的产出数量与期望不符。], [违约的总线或模块；期望数量与实际数量。],
)

至此结构定形、参数落定、清单装配完毕。剩下的问题是把结果安置进模块层次——下一章。
