#import "../lib.typ": *

= 动机：接口参数是拓扑的全局函数 <ch-motivation>

设计一颗 SoC，就是把几十个独立编写的模块——处理器核、缓存、DMA、内存控制器、外设——用若干共享协议连接成一个层次化的整体。本章论证一个贯穿全文的判断：*这些模块彼此相接的接口，其参数不是任何单个模块的局部属性，而是整张连接图的全局函数*。承认这一点之后，"参数协商"就不再是工程上的杂务，而应当成为硬件生成流程中一个显式的、可独立运行与检验的编译阶段。这正是 Syntheke 的出发点。

== 一个例子与三个参数 <sec-three-params>

考察下面这个刻意简化的 SoC：三个能发起访问的部件（两个核与一个 DMA）经交叉开关汇聚，向下到达二级缓存与内存控制器，另有一条低速分支通向外设总线上的 UART。

#图([示例 SoC。图中每条连线的硬件形状——位宽、地址检查逻辑、事务标识位数——都无法由连线两端的模块单独决定。])[
  #syn-diagram(
    node((0, 0), [核 0], name: <c0>),
    node((0, 1), [核 1], name: <c1>),
    node((0, 2), [DMA], name: <dma>),
    node((1, 1), [交叉开关], name: <xbar>),
    node((2, 0.4), [二级缓存], name: <l2>),
    node((3, 0.4), [内存控制器], name: <dram>),
    node((2, 1.6), [外设总线], name: <pbus>),
    node((3, 1.6), [UART], name: <uart>),
    edge(<c0>, <xbar>, "-|>"),
    edge(<c1>, <xbar>, "-|>"),
    edge(<dma>, <xbar>, "-|>"),
    edge(<xbar>, <l2>, "-|>"),
    edge(<l2>, <dram>, "-|>"),
    edge(<xbar>, <pbus>, "-|>"),
    edge(<pbus>, <uart>, "-|>"),
  )
]

沿着这张图看三个最普通的接口参数。

*数据位宽。*内存控制器希望以 256 位的数据通道饱和 DRAM 带宽；UART 的寄存器只有 32 位。核的一次缓存行重填共 512 位，走向内存控制器时拆成 2 拍，走向外设总线时拆成 16 拍。于是交叉开关到二级缓存一段的位宽取决于会流经这一段的所有发起者与所有目的地的组合；把内存控制器的通道从 256 位改成 128 位，受影响的不只是它自己的端口，还有二级缓存两侧、交叉开关下行侧乃至每个核的总线接口中与拍数相关的一切逻辑。

*地址集合。*交叉开关要把每个访问路由到正确的下游，其路由表是全部下游设备地址集合的不交并；每个核的合法地址检查（例如物理内存属性表）又是它*可达*的所有设备地址集合的并。新增一个外设，改动的不是那一个外设：它上游每一级路由器的路由表、每一个能访问它的发起者的地址检查，都随之变化。信息沿数据流动方向的*逆向*汇聚上来。

*事务标识空间。*多个发起者的请求汇入同一个下游端口后，返回的应答必须能找到原路。通行做法是在每个汇聚点为每个上游分配一段不相交的标识区间。设核 0 支持 4 个并发事务、核 1 支持 6 个，则汇聚后共需 10 个标识，端口需要 $ceil(log_2 10) = 4$ 位标识线；若核 1 的并发度改为 13，总数变为 17，下游*每一个*共享该路径的端口的标识位宽都从 4 位变成 5 位。

#图([汇聚点的标识区间分配。端点的标识位宽由上游全体并发度之和决定，是典型的"全局函数"参数。])[
  #syn-canvas({
    import cetz.draw: *
    // 数轴
    line((0, 0), (10.4, 0), mark: (end: ">"))
    // 区间 A [0,4)
    rect((0, 0), (4, 0.55), fill: rgb("#dbe9fb"), stroke: 0.6pt)
    content((2, 0.28), [核 0：`[0, 4)`])
    // 区间 B [4,10)
    rect((4, 0), (10, 0.55), fill: rgb("#fbe3df"), stroke: 0.6pt)
    content((7, 0.28), [核 1：`[4, 10)`])
    // 刻度
    for (x, t) in ((0, [0]), (4, [4]), (10, [10])) {
      line((x, 0), (x, -0.18))
      content((x, -0.45), t)
    }
    content((5.2, -1.1), [端口标识位宽 $= ceil(log_2 10) = 4$ 位])
  })
]

三个参数，三种流向：位宽这类"能力"要在发起者与目的地之间*取齐*；地址这类"供给"沿图*向上*汇聚；标识这类"资源"在汇聚处*向下*分割。共同点只有一个——*它们都不属于某个模块，而属于整张图*。任何一个端点的改动都可能波及与它相隔多跳的接口。

== 两种朴素方案，两种失败方式 <sec-two-failures>

面对全局参数，工程上有两条常见的路，它们都在规模面前失败，且失败方式不同。

*把参数写进构造函数，逐层手工传递。*每个模块把它需要知道的一切列为构造参数，由上层计算后传入。这个方案在纸面上无懈可击，实践中有三个致命点。其一是重复：同一个事实（比如"内存通道 256 位"）会以参数形式出现在从顶层到叶子的每一条路径上，声明量随层深与扇出的乘积增长。其二是真值来源涣散：当同一个事实出现在十处，十处不一致时没有任何机制指出哪一处是对的。其三是顺序死锁：A 的参数依赖 B 的属性，而 B 的构造又需要引用 A 的属性时，宿主语言的构造顺序强迫设计者"先假定一个值、再在运行期以断言验证该假定"——假定与断言散布全库，任何重构都必须同步更新它们，成本与风险都极高。

*把参数放进一个全局配置对象，按键查询。*模块不再声明依赖，转而在需要时向一个隐式传播的键值环境查询。这消灭了逐层传递的样板，也消灭了可读性：模块的签名不再讲出它依赖什么，读者必须通读实现才能列出它查询过的键；配置键可以在任意作用域被覆写，一次覆写的影响半径没有边界。仍以位宽为例："内存通道 256 位"一旦成为配置键，外设子系统的作者为了本地仿真便利，在自己的作用域把它覆写成 64 位；集成之后上层查到 256、深处查到 64——两处各自"正确"，系统层面错误，而错误首先显现于深处的某个断言，与该覆写相隔整棵调用树。配置与真实拓扑不一致时，错误总在*使用点*而非声明点被触发；而由于任何模块都可能查询任何键，子系统无法在隔离环境中构造与测试。

两种方案的失败可以归结为同一句话：它们都试图把一个*图上的计算*伪装成*树上的传参*。参数真正的依赖结构是连接图，而不是模块层次树；沿树传播图上的信息，要么重复（穿线），要么失控（全局环境）。

== 把协商变成一个显式阶段 <sec-explicit-phase>

既然接口参数是图的函数，正确的做法就是承认它：

+ 把连接图本身表达为一等公民的数据——哪些端点相连、以何种协议相连、各端给出何种参数；
+ 在图上运行一个确定性的算法，让参数沿边双向流动、在每条边上结算出双方都接受的最终值；
+ 只有当每条边都结算完毕，才允许触碰硬件——此时每个模块的接口形状已经完全确定，硬件生成器拿到的是一份不再变动的参数。

这三步的关系像条约的订立：各方先*起草*条款，再*协商*成文，最后*履行*。Syntheke（希腊语 συνθήκη，"条约"）的名字与全部结构都来自这个类比。它刻意不做的事情同样重要：Syntheke 本身*不构造任何电路*。硬件构造委托给 zaozi 生成器#footnote[zaozi 是一个独立的硬件构造库：Scala 3 编写，经 MLIR/CIRCT 直接产出 FIRRTL。不了解它不影响读本文档——Syntheke 对硬件侧只假设一件事：给定一份参数，生成器确定性地生成一个模块。这个假设的完整展开在 @sec-generator-contract。]；Syntheke 只负责三件事：例化哪些生成器、以何种参数例化、它们之间如何连接。协商与硬件构造之间只通过可序列化的参数交换数据，因此协商可以脱离任何硬件环境单独运行、单独测试、把结果存档。

== 需求清单 <sec-requirements>

以上论证收敛为八条需求。后续各章的每个设计决定都会回指它们。

#table(
  columns: (auto, 1fr),
  table.header([编号], [需求]),
  [R1], [*显式拓扑。*连接图是可遍历、可导出的普通数据，而非散落在对象字段中的隐式状态。],
  [R2], [*双向参数。*参数沿边双向流动：下行方向描述发起端的构成与行为，上行方向描述响应端的构成与能力；两者在边上结算为最终参数。],
  [R3], [*基数可推断。*端口数量本身常是全局函数（交叉开关的端口数等于接入者数量）。必须允许"数量待定"的连接声明，由框架解析出确切边数。],
  [R4], [*纯协商。*协商是纯函数：同一输入必得同一输出。协商失败不抛异常，而是带回一份完整的问题清单——所有错误一次看完，不在第一处停下。],
  [R5], [*相位分离。*构建（表达意图）、协商（结算参数）、例化（生成硬件）三阶段单向依赖。任何"在协商完成前读取协商结果"的程序都必须无法表达。],
  [R6], [*层次无关。*相连的两个模块不必是父子——隔着多层模块边界也能直接书写连接。若非如此，跨子树的每条连接都必须手工逐层转发端口，模块层次结构将完全受连接关系支配。沿途每一层的端口由框架规划生成，不由人手写。],
  [R7], [*验证一等。*协同仿真、断言这类验证逻辑需要观察设计深处的信号；直接从模块内部引出信号会破坏封装，在顶层以层次路径字符串引用信号则极其脆弱。因此观测点同样建模为图上的连接，复用同一套自动生成端口的机制；发布构建移除验证逻辑时只需整层关闭，无需在代码库中逐处排查残留。],
  [R8], [*可序列化边界。*跨入硬件生成器的参数必须可序列化——这是复现、缓存与分布式构建的前提。],
)

下一章给出承载这八条需求的概念模型。
