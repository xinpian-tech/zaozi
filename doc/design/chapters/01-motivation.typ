#import "../lib.typ": *

= SoC 生成器的设计需求分析 <ch-motivation>

SoC 设计把处理器核、缓存、DMA、内存控制器和外设等独立模块，通过多种协议集成为层次化系统。这些模块通常都是参数化 IP，连接口本身也是参数化的。各模块单独实现完成后，模块之间仍可能因为参数不匹配而引入协议错误。

根本原因在于：IP 生成器的参数不是由单个模块独立决定的，而是由整个 SoC，甚至 Chiplet、Chip-to-Chip 级别设计的连接图共同决定的。模块之间的连接关系影响全局功能和约束，因此参数生成与参数校验必须在系统级完成。

Syntheke 用基于 Scala 的嵌入式领域专用语言（embedded domain-specific language，eDSL）描述 SoC：处理器、DMA、存储、外设、Xbar（crossbar）、NoC、时钟树和电源网格都是设计里显式例化的模块，每个模块的硬件由一个生成器按参数产生；模块之间用显式的连接相连；框架先在连接图上推导出每个模块的接口参数，然后才生成硬件。概念模型见 @ch-model；与 Diplomacy 的关系见 @sec-diplomacy。

从定位上看，Syntheke 与 SystemRDL 等 SoC 集成专用语言相似，都是为了描述和生成复杂 SoC 的系统结构；作为 Scala eDSL，它有更高的灵活性和抽象能力。Syntheke 还把时钟、复位、电源和 IO 这些物理设计的关键需求，建模为带有相应协议的显式模块，纳入同一套集成结构。

== 一个例子：位宽、地址与事务身份 <sec-three-params>

考察下面这个简化的 SoC：三个能发起访问的部件（两个核与一个 DMA）经 crossbar 汇聚，向下到达二级缓存与内存控制器，另有一条低速分支通向外设互连上的 UART。

#图([示例 SoC。图中每条连线的位宽、地址检查逻辑和事务标识位数都由整张连接图决定。])[
  #syn-diagram(
    node((0, 0), [核 0], name: <c0>),
    node((0, 1), [核 1], name: <c1>),
    node((0, 2), [DMA], name: <dma>),
    node((1, 1), [crossbar], name: <xbar>),
    node((2, 0.4), [二级缓存], name: <l2>),
    node((3, 0.4), [内存控制器], name: <dram>),
    node((2, 1.6), [外设互连], name: <pic>),
    node((3, 1.6), [UART], name: <uart>),
    edge(<c0>, <xbar>, "-|>"),
    edge(<c1>, <xbar>, "-|>"),
    edge(<dma>, <xbar>, "-|>"),
    edge(<xbar>, <l2>, "-|>"),
    edge(<l2>, <dram>, "-|>"),
    edge(<xbar>, <pic>, "-|>"),
    edge(<pic>, <uart>, "-|>"),
  )
]

以下三类参数说明这种全局依赖。

*数据位宽。*内存控制器采用 256 位数据通道；UART 寄存器接口为 32 位。处理器核访问 UART 时发出绕过缓存的 MMIO 窄事务，按窄事务直接传输。缓存行重填需要分多个传输拍完成：512 位缓存行经 256 位通道传输需要 2 拍，经 128 位通道传输需要 4 拍。因此，crossbar 到二级缓存链路的位宽取决于所有可能经过该链路的发起者与目的地。内存通道由 256 位改为 128 位时，内存控制器端口、二级缓存两侧、crossbar 后续端口及处理器核内与拍数相关的逻辑均会改变。

*地址集合。*crossbar 根据全部目的设备地址集合的不交并形成路由表；每个核的合法地址检查（例如物理内存属性表）则使用该核可达设备地址集合的并集。新增外设会改变通往该外设的各级路由表以及所有可达发起者的地址检查。地址信息沿数据流动方向逆向汇聚。

*事务身份与返回路由。*片上互连协议中的 ID 字段并不只表达一件事，而是共同服务于几类需求：区分哪个节点发起或接收事务，区分同一节点同时存在的多个事务，把应答送回原发起者，以及索引互连模块内部的缓冲、重排或转发表项。不同协议会把这些需求分摊到不同字段中；Syntheke 需要协商的是这些身份需求和资源约束。

以 AXI 为例，请求在 `AWID` 与 `ARID` 中携带事务 ID，应答在 `BID` 与 `RID` 中返回同一 ID。这个 ID 只在单个发起者接口内有意义；两个发起者可以同时使用相同的本地 ID。多个发起者经互连模块汇入同一个响应者时，互连模块必须让返回应答能够找到原发起者。一种静态做法是在下游 AXI ID 中拼入发起者编号：设核 0 使用 2 位本地 ID，核 1 使用 3 位本地 ID，则互连模块可生成 4 位下游 ID，其中高位标识发起者，低位保留本地 ID。响应返回后，互连模块根据高位选择返回端口，并把低位还原为对应发起者看到的本地 ID。若下游只能接受 3 位 ID，静态拼接不可行，要改用表项式重映射等策略。上游有哪些身份需求、互连模块能实现哪些策略、下游能接受几位 ID，三方齐了才能选定策略；框架把这一步放在逐条连接求解时完成（@sec-settle-pp），选不出可行策略就报告能力不足。

#图([AXI ID 静态重映射示例。下游 4 位 ID 的高位编码发起者，低位保留各发起者的本地 ID；返回时互连模块反向解码。])[
  #syn-canvas({
    import cetz.draw: *
    // 数轴
    line((0, 0), (10.4, 0), mark: (end: ">"))
    // 核 0: prefix 0, local ID 2 bits
    rect((0, 0), (2.6, 0.55), fill: rgb("#dbe9fb"), stroke: 0.6pt)
    content((1.3, 0.28), [核 0：`0b0_000..0b0_011`])
    // 未使用间隙
    rect((2.6, 0), (5.2, 0.55), fill: rgb("#f4f4f4"), stroke: 0.4pt)
    content((3.9, 0.28), text(size: 8pt, fill: c-dim)[保留])
    // 核 1: prefix 1, local ID 3 bits
    rect((5.2, 0), (10.4, 0.55), fill: rgb("#fbe3df"), stroke: 0.6pt)
    content((7.8, 0.28), [核 1：`0b1_000..0b1_111`])
    // 刻度
    for (x, t) in ((0, [0]), (2.6, [4]), (5.2, [8]), (10.4, [16])) {
      line((x, 0), (x, -0.18))
      content((x, -0.45), t)
    }
    content((5.2, -1.1), [下游 AXI ID 位宽 $= 1 + max(2, 3) = 4$ 位])
  })
]

再以 CHI 为例，协议把节点身份和事务标签拆开表达：`SrcID` 与 `TgtID` 标识发送节点和目标节点，`TxnID` 只需要在同一个 `SrcID` 下唯一；转发相关事务还会用 `FwdNID` 与 `FwdTxnID` 携带原始请求者的节点与事务标签，部分数据流程还会用 `DBID` 表示数据缓冲资源。因此，两个请求节点可以使用相同的 `TxnID`，只要它们的 `SrcID` 不同，系统仍能区分事务。对这类协议，协商重点不再是把多个本地 ID 拼成一个下游 ID，而是分配全局节点编号，校验各节点的事务标签空间，并在代理、桥或协议转换模块隐藏多个请求者时，确定是否需要事务标签虚拟化或表项式返回路由。

这些参数采用不同的传播与合并规则：位宽能力在发起者与目的地之间取交集，地址集合逆向汇聚，事务身份需求由协议和互连模块能力共同约束，并转化为 ID 位宽、节点编号、返回路由或内部表项容量等要求。它们均为连接图的属性；一个端点的变化可能影响多跳之外的接口形状和连接合法性。

连接的两端各自持有一半事实：发起侧给出要求和能力，响应侧给出供给和限制。中断、时钟、复位、电源、Debug、Trace 与 MBIST 都是同一形状。@ch-protocol 定义统一的参数类型和求解流程。

== 显式协商阶段 <sec-explicit-phase>

接口参数是连接图的函数，因此参数协商应当是硬件生成流程中一个显式的、可单独运行和检验的阶段。流程分三步：先把模块、连接和各端的参数声明记录成显式数据，这一步称为构建；然后在这些数据上推导每条连接的最终参数，称为协商；全部连接都求解成功后，才把每个模块的全部参数交给硬件生成器生成电路，称为例化。三个阶段的划分见 @sec-triptych。

Syntheke 只负责选择生成器、算出交给每个生成器的参数、规划模块之间的端口与连线；硬件逻辑由 zaozi 生成器#footnote[zaozi 是独立的硬件构造库：使用 Scala 3，并通过基于 MLIR 的 CIRCT 产出 FIRRTL。生成器契约见 @sec-generator-contract。]产出。协商可单独执行和测试；协商数据可按 @sec-export 导出；交给生成器的参数可序列化归档（@sec-serialization-boundary）。

== 详细需求分析 <sec-requirements>

以下是对目前 SoC 设计过程中的痛点分析：

#需求([迭代提速])[
  传统 SoC 集成的一轮改动要经过手工连线、手工更新架构图、再靠仿真发现集成错误，周期以周计。需要让设计源码是唯一的实施来源；架构图从源码导出；在协商期检查 SoC 参数不匹配、连接遗漏与数量错误等问题。
] <req-iteration>

#需求([参数协商])[
  位宽、地址、标识、频率等接口参数由多个模块的端口共同决定，人工对齐必然出错，而且发现得晚。要求：这类参数由各端的声明出发、由框架推导确定；不一致的在协商期报错，不进入电路。
] <req-negotiation>

#需求([互连实现显式声明])[
  每一段互连由哪个生成器实现、用什么拓扑参数，必须写在设计源码里，例如 Xbar、NoC、直连、时钟树或电源网格，框架不隐式生成互连。要求：互连模块与其它模块用同一套连接与协商机制；互连实现承载不了协商出的参数时，在协商期报错。
] <req-interconnect>

#需求([任意层次连接])[
  相连的两个模块常常不是父子；手工写出连接沿途每一层的端口，拓扑一改就要改一串。要求：连接可以跨越任意数量的模块边界；沿途各层的端口由框架规划生成。
] <req-hierarchy>

#需求([验证观测自动接入])[
  协同仿真、断言等验证逻辑要观察设计内部信号；手工从模块内部引线破坏封装、拓扑一改就返工，在顶层用层次路径字符串引用信号又极脆弱。要求：声明探针即完成接线，观测路径随拓扑自动更新；发布构建通过 FIRRTL 层#footnote[FIRRTL 层（layer）是 FIRRTL 的一种条件编译机制：编译流程可以移除标注在指定层里的硬件，并逐位保留层外电路。Syntheke 把声明放入对应的层，展开见 @sec-layers。]选择保留哪些验证逻辑。
] <req-verification>

#需求([IP 独立例化与测试])[
  每个 IP 必须能以一份固定的参数文件独立例化、独立测试、可复现——集成问题与 IP 内部问题分开定位，IP 团队在隔离环境中工作。
] <req-ip>
