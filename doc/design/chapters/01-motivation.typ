#import "../lib.typ": *

= SoC 生成器的设计需求分析 <ch-motivation>

SoC 设计把处理器核、缓存、DMA、内存控制器和外设等独立模块，通过多种协议集成为层次化系统。
这些设计模块通常都是参数化 IP，甚至连接口本身也是参数化的。
因此，在完成各个模块的参数化实现后，不同模块之间可能因为参数不匹配而引入潜在的协议 Bug。

这一需求的本质原因在于：IP 生成器的参数并不是由单个模块独立决定的，而是由整个 SoC，甚至 Chiplet、Chip-to-Chip 级别设计的连接图共同决定的。
模块之间的互联关系会影响全局功能和约束，因此参数生成与参数校验必须在系统级完成。

为了解决这一问题，Syntheke 采用了基于 Scala 的嵌入式领域专用语言，即 embedded domain-specific language，eDSL。
它将 SoC 的构建过程表达为显式模块及其节点之间的连接：处理器、DMA、存储、外设、Xbar、NoC、时钟树和电源网格都作为生成器模块出现。
每个模块节点表示一个具名端口，一次 bind 连接一个输出节点和一个输入节点。bind 与模块内部显式声明的参数依赖共同形成两张方向相反的 DAG；协商器在其上正向传播 `Down`、反向传播 `Up`，再逐条边求解，并据此确定电路生成器的完整参数。

这一核心思想源于 Diplomacy，但 Syntheke 对其进行了有针对性的约束和简化：
- *保留参数依赖图上的 `Down` / `Up` 双向传播与逐边求解*
- *强制要求 IP 模块可序列化，并使用 Zaozi 作为生成器语言*
- *去掉 AOP（Aspect-Oriented Programming）带来的过高自由度*
- *去掉 `resolveStar` 与四种基数算子；端口数量由具名节点的显式声明给出，每个节点恰好参与一次 bind*
这样做的目的是减少隐式行为，使模块边界、连接关系和生成过程更加明确，并解决 Diplomacy 在多层次耦合下带来的几个根本问题：
- 验证成本过高；
- 后端设计无法切分边界；
- 无法把 NoC 作为生成器模块纳入同一套集成流程；

从定位上看，Syntheke 与 SystemRDL 等 SoC 集成专用语言有相似之处，都是为了描述和生成复杂 SoC 系统结构。
但由于 Syntheke 是基于 Scala 的 eDSL，它具有更高的灵活性和抽象能力，能够提供更大的设计自由度。
同时，Syntheke 还将芯片物理设计中的关键需求，例如时钟、复位、电源和 IO，建模为对应协议下的显式生成器模块，并纳入 SoC 设计的整体集成结构之中。

== 一个例子：位宽、标识、地址与该地址对应的属性<sec-three-params>

考察下面这个简化的 SoC：三个能发起访问的部件（两个核与一个 DMA）经 crossbar 汇聚，向下到达二级缓存与内存控制器，另有一条低速分支通向外设互连上的 UART。

#图([示例 SoC。图中每条连线的位宽、地址检查逻辑和事务标识位数都由整张连接图决定。])[
  #syn-diagram(
    node((0, 0), [核 0], name: <c0>),
    node((0, 1), [核 1], name: <c1>),
    node((0, 2), [DMA], name: <dma>),
    node((1, 1), [crossbar], name: <xbar>),
    node((2, 0.4), [二级缓存], name: <l2>),
    node((3, 0.4), [内存控制器], name: <dram>),
    node((2, 1.6), [外设互连], name: <pic>),
    node((3, 1.6), [UART], name: <uart>),
    edge(<c0>, <xbar>, "-|>"),
    edge(<c1>, <xbar>, "-|>"),
    edge(<dma>, <xbar>, "-|>"),
    edge(<xbar>, <l2>, "-|>"),
    edge(<l2>, <dram>, "-|>"),
    edge(<xbar>, <pic>, "-|>"),
    edge(<pic>, <uart>, "-|>"),
  )
]

以下三类参数说明这种全局依赖。

*数据位宽。*内存控制器采用 256 位数据通道；UART 寄存器接口为 32 位。处理器核访问 UART 时发出绕过缓存的 MMIO 窄事务，按窄事务直接传输。缓存行重填需要分多个传输拍完成：512 位缓存行经 256 位通道传输需要 2 拍，经 128 位通道传输需要 4 拍。因此，crossbar 到二级缓存链路的位宽取决于所有可能经过该链路的发起者与目的地。内存通道由 256 位改为 128 位时，内存控制器端口、二级缓存两侧、crossbar 后续端口及处理器核内与拍数相关的逻辑均会改变。

*地址集合。*crossbar 根据全部目的设备地址集合的不交并形成路由表；每个核的合法地址检查（例如物理内存属性表）则使用该核可达设备地址集合的并集。新增外设会改变通往该外设的各级路由表以及所有可达发起者的地址检查。地址信息沿数据流动方向逆向汇聚。

*事务身份与返回路由。*片上互连协议中的 ID 字段并不只表达一件事，而是共同服务于几类需求：区分哪个节点发起或接收事务，区分同一节点同时存在的多个事务，把应答送回原发起者，以及索引互连模块内部的缓冲、重排或转发表项。不同协议会把这些需求分摊到不同字段中；Syntheke 需要协商的是这些身份需求和资源约束，而不是强行把所有协议归约成同一种 ID 形式。

以 AXI 为例，请求在 `AWID` 与 `ARID` 中携带事务 ID，应答在 `BID` 与 `RID` 中返回同一 ID。这个 ID 只在单个发起者接口内有意义；两个发起者可以同时使用相同的本地 ID。多个发起者经互连模块汇入同一个响应者时，互连模块必须让返回应答能够找到原发起者。一种静态做法是在下游 AXI ID 中拼入发起者编号：设核 0 使用 2 位本地 ID，核 1 使用 3 位本地 ID，则互连模块可生成 4 位下游 ID，其中高位标识发起者，低位保留本地 ID。响应返回后，互连模块根据高位选择返回端口，并把低位还原为对应发起者看到的本地 ID。若下游响应者只能接受 3 位 ID，互连模块就必须选择表项式重映射等其它策略，并校验内部表项容量；否则应在协商期报告能力不足。

#图([AXI ID 静态重映射示例。下游 4 位 ID 的高位编码发起者，低位保留各发起者的本地 ID；返回时互连模块反向解码。])[
  #syn-canvas({
    import cetz.draw: *
    // 数轴
    line((0, 0), (10.4, 0), mark: (end: ">"))
    // 核 0: prefix 0, local ID 2 bits
    rect((0, 0), (2.6, 0.55), fill: rgb("#dbe9fb"), stroke: 0.6pt)
    content((1.3, 0.28), [核 0：`0b0_000..0b0_011`])
    // 未使用间隙
    rect((2.6, 0), (5.2, 0.55), fill: rgb("#f4f4f4"), stroke: 0.4pt)
    content((3.9, 0.28), text(size: 8pt, fill: c-dim)[保留])
    // 核 1: prefix 1, local ID 3 bits
    rect((5.2, 0), (10.4, 0.55), fill: rgb("#fbe3df"), stroke: 0.6pt)
    content((7.8, 0.28), [核 1：`0b1_000..0b1_111`])
    // 刻度
    for (x, t) in ((0, [0]), (2.6, [4]), (5.2, [8]), (10.4, [16])) {
      line((x, 0), (x, -0.18))
      content((x, -0.45), t)
    }
    content((5.2, -1.1), [下游 AXI ID 位宽 $= 1 + max(2, 3) = 4$ 位])
  })
]

再以 CHI 为例，协议把节点身份和事务标签拆开表达：`SrcID` 与 `TgtID` 标识发送节点和目标节点，`TxnID` 只需要在同一个 `SrcID` 下唯一；转发相关事务还会用 `FwdNID` 与 `FwdTxnID` 携带原始请求者的节点与事务标签，部分数据流程还会用 `DBID` 表示数据缓冲资源。因此，两个请求节点可以使用相同的 `TxnID`，只要它们的 `SrcID` 不同，系统仍能区分事务。对这类协议，协商重点不再是把多个本地 ID 拼成一个下游 ID，而是分配全局节点编号，校验各节点的事务标签空间，并在代理、桥或协议转换模块隐藏多个请求者时，确定是否需要事务标签虚拟化或表项式返回路由。

这些参数采用不同的传播与合并规则：位宽能力在发起者与目的地之间取交集，地址集合逆向汇聚，事务身份需求由协议和互连模块能力共同约束，并转化为 ID 位宽、节点编号、返回路由或内部表项容量等要求。它们均为连接图的属性；一个端点的变化可能影响多跳之外的接口形状和连接合法性。

连接边界上的输出节点提供初始 `Down`，输入节点提供初始 `Up`；中断、时钟、复位、电源、Debug、Trace 与 MBIST 均采用这一机制。#ref(<ch-protocol>)定义统一的参数类型和求解流程。

== 显式协商阶段 <sec-explicit-phase>

接口参数是连接图的函数，因此 Syntheke 在连接图上执行三步处理：

+ 用显式数据结构记录输入节点、输出节点、bind、模块内部参数依赖以及连接边界的初始参数；
+ 对参数依赖 DAG 做拓扑排序，按拓扑序传播 `Down`、按反向拓扑序传播 `Up`，再逐条求出连接的最终参数；
+ 所有边求解成功后方可进入例化；此时模块接口已经确定，框架把构建期参数与协商结果合成为不可变的完整参数，硬件生成器以该参数为输入。

Syntheke 选择生成器、计算完整参数并生成端口与连线计划；硬件逻辑由 zaozi 生成器#footnote[zaozi 是独立的硬件构造库：使用 Scala 3，并通过基于 MLIR 的 CIRCT 产出 FIRRTL。生成器契约见 @sec-generator-contract。]产出。协商可单独执行和测试；协商数据可按 @sec-export 导出，完整参数可序列化归档。

== 六条需求 <sec-requirements>

设计需求分为以下六项：

#需求([迭代提速])[
  SoC 用 eDSL 定义，设计源码是唯一的实施来源；架构图从源码导出；构建产生的设计规格中可静态检查的参数不匹配、连接遗漏与数量错误在协商期间报出，生成器实现与接口契约的失配在例化入口报告。
] <req-iteration>

#需求([参数协商])[
  SoC 的位宽、地址、标识与频率等接口参数由多个模块端口共同约束。要求：模块显式声明输入节点到输出节点的参数依赖；输出节点的 `dFn` 只读取所声明前驱输入节点的 `Down` 与构建期参数，输入节点的 `uFn` 只读取所声明后继输出节点的 `Up` 与构建期参数。bind 与这些模块内部参数依赖组成方向相反的 `Down`、`Up` 参数依赖 DAG；两遍传播结束后，每条边以自己的 `Down` 与 `Up` 独立求出 `Edge`。传播或逐边求解发现冲突时，以协商错误结束当前流水线。
] <req-negotiation>

#需求([任意层次连接])[
  连接可以跨越任意数量的模块边界；框架为跨子树连接规划并生成沿途各层端口。
] <req-hierarchy>

#需求([验证观测自动接入])[
  协同仿真、断言等验证逻辑通过探针声明观察设计内部信号。要求：框架根据探针声明和模块拓扑生成跨层连接，并随拓扑更新观测路径；发布构建通过 FIRRTL 层#footnote[FIRRTL 层（layer）是 FIRRTL 的一种条件编译机制：编译流程可以移除标注在指定层里的硬件，并逐位保留层外电路。Syntheke 把声明放入对应的层，展开见 @sec-layers。]选择验证逻辑集合。
] <req-verification>

#需求([IP 独立例化与测试])[
  每个 IP 必须能以一份固定的参数文件独立例化、独立测试、可复现——集成问题与 IP 内部问题分开定位，IP 团队在隔离环境中工作。
] <req-ip>

#需求([互连实现显式声明])[
  每一段互连必须在设计源码中显式例化对应生成器模块，例如 Xbar、NoC、直连、时钟树或电源网格，并给出其拓扑用户参数。可达关系、地址译码、事务身份以及时钟与电源关系通过模块输入、输出节点之间的普通 `dFn`、`uFn` 传播，最终体现在各条已求解边和生成器协议参数中。要求：所有生成器模块使用同一套节点、bind、双向传播和逐边求解机制；生成器能力不能承载本模块求得的参数时在协商流水线中报错。
] <req-interconnect>
