#import "../lib.typ": *

= 互连模型 <ch-interconnect>

一段互连承载着一套固有的逻辑结构:哪些部件能发起访问、各自能到达哪些部件;访问按什么地址译码到目的地;互连的每一部分属于哪个时钟域与电源域。这套结构由架构决定,是设计的固定事实——无论最终用什么硬件承载,它都同样成立,因此应当描述一次、始终保留。而由哪种硬件来承载它——交叉开关、片上网络、还是一组直连,乃至一个片上网络内部如何再分区——是性质完全不同的另一个决定:它服从面积、频率与带宽的权衡,会随工艺与性能数据在设计过程中反复更改,因此必须能独立选择、独立替换。

传统流程把两件事写在同一处:连接结构直接表达为对某个具体互连部件的例化与逐端口连线,逻辑事实散落在互连实现的参数与连线里。于是"换一种互连"不是换一个部件,而是重写连接结构本身——原本不该变的那部分被迫跟着变。#ref(<req-interconnect>)要求消除这种纠缠。本章给出实现分离的机制:逻辑结构落在总线上,硬件承载落在物理互连上;前者协商出全部参数,后者只消费协商的结果;两者之间只有一次单向的数据交接(@sec-manifest)。

== 总线与物理互连:两件分开的事 <sec-bus-fabric>

一条#term[总线][bus]是一个逻辑分组:它命名一组#term[成员][member],并承载关于这组成员的全部逻辑事实——谁能到达谁、地址如何译码、各成员属于哪个时钟域与电源域。参数协商就在总线的成员之上运行(@ch-negotiation):成员的声明在总线上相遇,结算出每个成员的接口与整张地址图。总线自身不产生任何硬件——它没有端口、没有连线,是纯粹的规格对象。

一个#term[物理互连][fabric]则是硬件的来源:一个消费总线协商结果、产出互连硬件的生成器。交叉开关、片上网络、一组点对点直连,都是物理互连的具体形态。

#不变量[一条总线恰好对应一个物理互连:总线在构造时就绑定它的 fabric 策略;设计中有多少条总线,就有多少个物理互连。]

绑定发生在构造时,原因在于具名节点(@sec-placement)属于物理互连:附着行要引用节点的名字,而暴露哪些节点由 fabric 策略给出,所以策略必须先于第一行附着确定:

```scala
val periph = Bus("periph", fabric = Crossbar())        // 由交叉开关承载
val sys    = Bus("sys",    fabric = Noc(Mesh(2, 2)))   // 由 2×2 网格片上网络承载
```

分离的动机在于参数的性质。位宽、地址图、标识空间、域归属是逻辑结构的属性:它们由成员的声明与"谁到达谁"的关系决定,与承载硬件无关——同一组成员,交给交叉开关与交给片上网络,算出的地址图与接口一模一样。计算既然相同,就应当只做一次:在总线上算,把结果交给承载这条总线的那个物理互连(@sec-manifest)。这一步分离之后,交叉开关与片上网络之间的替换只触及物理互连这一个对象;总线上的成员、地址与协商结果原地不动,附着行(@sec-attach)也不必改写。

#图([总线与物理互连。虚线框是逻辑总线:成员的集合与它们的地址、可达、域事实,不产硬件。实线框是兑现这条总线的物理互连:互连本体,外加每个成员恰好一个端口(小方块)。])[
  #syn-canvas({
    import cetz.draw: *
    let mx = (0.4, 3.1, 5.8, 8.5)
    let labs = ([cpu], [dma], [dram], [uart])
    for i in range(4) {
      let x = mx.at(i)
      rect((x, 2.7), (x + 1.9, 3.45), stroke: 0.7pt, radius: 0.08)
      content((x + 0.95, 3.075), labs.at(i))
      line((x + 0.95, 2.7), (x + 0.95, 2.05), stroke: 0.7pt)
      rect((x + 0.75, 0.62), (x + 1.15, 1.02), stroke: 0.6pt, fill: c-fill)
    }
    rect((0, 1.3), (10.8, 2.05), stroke: (dash: "dashed", thickness: 0.8pt), radius: 0.1)
    content((5.4, 1.675), [总线 `sys`(逻辑):成员集合 · 地址译码 · 可达关系 · 域归属])
    line((5.4, 1.3), (5.4, 0.86), mark: (end: ">"), stroke: 1pt)
    content((6.55, 1.16), text(size: 8pt, fill: c-dim)[兑现(一对一)])
    rect((0, -0.55), (10.8, 0.82), stroke: 1pt, radius: 0.1)
    content((5.4, 0.02), [物理互连(交叉开关 / 片上网络 / 直连):承载 `sys` 的互连硬件])
  })
]

== 附着:唯一的连接原语 <sec-attach>

把成员接入总线只有一种写法,称为#term[附着][attach]:

```scala
sys.node("n_mem") <~ dram
```

`sys.node("n_mem")` 取出 `sys` 的物理互连中名为 `n_mem` 的#term[具名节点][named node]——物理互连对外暴露的一个物理落点,一个端口或一个路由器(@sec-placement);`<~` 把成员 `dram` 附着在这个节点上。附着是本模型唯一的连接原语:设计的整张连接结构,就是全部附着行的集合。

成员有两种形态,写法只有一种。成员要么是叶子设备——处理器、存储控制器、外设这类不再分解的部件;要么是一条子总线(@sec-flat-nest)。二者以同一语法附着,对总线完全同质:一条子总线就是一个成员,占一个具名节点,与一个设备无异。这种同质性不是语法上的便利,而是嵌套结构的基础——父总线不需要为"成员是子总线"设任何特例,清单的成员表(@sec-manifest)也因此对二者只有一种表示。

数量就是附着的个数。一条总线有多少成员——它的物理互连要提供多少端口——答案就是数一数附着行。数量没有独立的声明:增加一个设备就是增加一行附着,删除一个设备就是删除一行,一切计数随之自动正确。数量不可能与事实失配,因为它没有第二处表达。

方向由成员给出。`<~` 记录哪一端是发起者、哪一端是响应者,而这来自成员自身的角色——处理器声明自己发起访问,存储声明自己响应——不来自节点:节点只是位置,发起者与响应者都经它接入;某个 fabric 若限定某节点只接特定方向,那属于它自己的能力约束,在能力校验时裁决(@sec-manifest)。协商的双向参数流(@ch-negotiation)沿这个方向定向。

一个成员对应它模块侧的一个节点;一个生成器模块可以持有多个节点,分别附着到不同总线——例如一个 DMA 以数据接口接入系统总线、以配置接口接入外设总线,那是两个节点、两次独立的附着。

=== 落点:具名节点 <sec-placement>

附着行里的名字把成员放在物理互连的一个具体#term[落点][placement]上。落点服从四条规则,每条各有动机。

*落点普适:每个物理互连都暴露具名节点。*任何物理互连都占物理面积,它的每个端口都是一个独立的物理位置与仲裁槽位——交叉开关的两个端口即便在可达性上完全对称,也位于版图的不同处、占仲裁的不同槽,并不可互换;片上网络的端口更是显然挂在不同的路由器上;直连 fabric 同样每个成员一个节点,名字标识那对连线的端点。因此不存在"无落点"的物理互连:落点不是片上网络的特有概念,只是在片上网络上更醒目。

*落点显式:fabric 绝不替成员挑节点。*若允许自动布点,落点就成了 fabric 内部的隐藏决定:换一个 fabric 实现、增删一条无关的附着,布点都可能悄悄变化,而挂在落点上的 floorplan 约束与仲裁配置(见下)随之失效,且没有任何一处规格改动能解释这次变化。落点必须出现在规格里,由写规格的人决定。

*身份是名字,不是编号。*一个节点的身份是例化 fabric 时赋予的名字。编号做不成普适身份:不同 fabric 的坐标空间天然不同——交叉开关的端口排成一维序列,网格片上网络的路由器是二维坐标,不规则拓扑的片上网络根本没有可移植的编号方案。名字是唯一对所有 fabric 都成立的身份形式。结构化的 fabric 可以在名字之上提供坐标糖:网格 fabric 的 `mesh(x, y)` 解析到对应路由器的具名节点——糖只是查名字的另一种拼法。

*落点只属于构建期与例化期。*落点是构建期的人为输入;协商不读它——成员落在哪个节点,既不改变它的接口,也不改变地址图,只改变事务在互连内部走的物理路径,而路由路径是 fabric 自己的事。落点原样穿过协商、抄入清单,在例化期被物理互连消费(@sec-ic-phases)。

显式不等于逐行手敲。落点映射是构建期的普通代码,可以对成员列表循环、由函数算出:

```scala
for ((core, i) <- cores.zipWithIndex)
  sys.node(s"n_core_$i") <~ core
```

这条规则约束的是决定者——落点由规格给出,绝不由 fabric 代劳——不约束写法的长短。

名字的回报是稳定身份:floorplan 约束按节点名下达,性能与拥塞报告按节点名组织,仲裁优先级按节点名配置。更换 fabric 时,附着的写法不变;要重新审视的是名字与新 fabric 的拓扑——新 fabric 暴露哪些节点、成员应当落在哪里。这正是换互连时本来就必须做的那个物理决定,机制把它显式摆上桌面,而不是藏进自动布点里。

#不变量[一个成员恰好附着一次,落在一个具名节点上;一个具名节点可承载一个或多个成员——共享同一路由器的若干成员各在其上占一个端口;空置的具名节点合法。]

#图([附着到具名节点。三行附着:`sys.node("n_cpu") <~ cpu`、`sys.node("n_mem") <~ dram`、`sys.node("n_periph") <~ periph`。实心圆是物理互连暴露的具名节点;成员落在节点上,方向来自成员的角色。])[
  #syn-canvas({
    import cetz.draw: *
    rect((0, 0), (10.4, 1.5), stroke: 1pt, radius: 0.1)
    content((5.2, 0.5), [`sys` 的物理互连])
    let px = (1.7, 5.2, 8.7)
    let plabs = ([`n_cpu`], [`n_mem`], [`n_periph`])
    let mlabs = ([cpu(发起者)], [dram(响应者)], [periph(子总线)])
    for i in range(3) {
      let x = px.at(i)
      circle((x, 1.5), radius: 0.13, fill: black)
      content((x, 1.12), text(size: 8pt, plabs.at(i)))
      rect((x - 1.25, 2.55), (x + 1.25, 3.3), stroke: 0.7pt, radius: 0.08)
      content((x, 2.925), mlabs.at(i))
      line((x, 2.55), (x, 1.7), mark: (end: ">"), stroke: 0.8pt)
    }
    content((6.05, 2.12), text(size: 8pt, fill: c-dim)[附着 `<~`])
  })
]

== 平连与嵌套 <sec-flat-nest>

*平连是默认形态。*一条总线,所有设备直接附着,由一个物理互连承载——@sec-bus-fabric 的图正是这个形状。设计的大部分连接结构就是这样一条或少数几条平的总线;嵌套是为特定边界准备的工具,不是常态。

*嵌套是同一机制的递归:成员可以是一条子总线。*子总线把内部成员保持为私有:父总线看不见它里面有哪些设备,只看见一个成员——内部各成员地址区域的并集、一个结算出的接口、一个占用的具名节点。内部成员之间的通信走子总线自己的物理互连,不经过父;内部成员与外部的通信经子总线朝父的接口;兄弟成员之间经父总线。

"一条总线一个物理互连"(@sec-bus-fabric)在每一层照常成立:子总线绑定自己的 fabric 策略、拥有自己的物理互连;父的物理互连把子总线当作一个普通端口。于是嵌套的设计是一棵总线树,同构于一棵物理互连树。这棵树属于连接结构,与模块层次树正交(@sec-two-graphs):总线按可达性与域组织成员,模块层次按所有权组织代码,一条总线的成员可以位于层次树的不同子树。

什么情况引入子总线?恰好三种边界,各有明确的收益:

+ *独立交付的子系统边界。*一个子系统要作为独立单元构建、验证,或复制多份接到不同环境(@req-ip)。子总线给它封闭的成员集合与一份自己的清单(@sec-manifest),于是它能脱离整机、以固定参数独立例化与测试(@sec-ic-phases)。

+ *时钟域或电源域改变的边界。*域穿越硬件需要一个确定的安放位置。把域边界画成总线边界,穿越就恰好落在子总线朝父的那个接口上——集中在一处,而不是散在每个跨域成员的路径上;域归属也随成员表交给两侧各自的物理互连。

+ *更换物理互连的边界。*一片区域要用与骨干不同的 fabric 兑现。典型情形:骨干是片上网络,而少数几个慢速外设不值得各占一对片上网络的出入端口——把它们收进一条子总线,用一个便宜的交叉开关承载,整组外设在骨干上只占一个端口。fabric 的选择粒度是总线(@sec-bus-fabric),所以"这一片换一种互连"必然表达为一条子总线。

反面规则同样明确:不要仅仅为了分组而建子总线。一组成员如果应当共用一个物理互连,就把它们直接附着上去——不带 fabric 边界的"分组"没有独立的表达,它就是这些成员共同附着的那条总线。每多一层子总线,就多一个物理互连实体与一道接口;这笔硬件开销必须由上述三种边界之一偿付,单纯的命名整洁不构成理由。

#图([平连(左)与嵌套(右)。右图中子总线 `periph` 以一个成员的身份占 `sys` 的一个节点;它的内部成员对 `sys` 不可见,由它自己的物理互连(交叉开关)承载。])[
  #syn-canvas({
    import cetz.draw: *
    // 左:平连
    rect((0, 2.2), (4.6, 2.9), stroke: (dash: "dashed", thickness: 0.8pt), radius: 0.1)
    content((2.3, 2.55), [总线 `sys`(平连)])
    let lx = (0.1, 1.65, 3.2)
    let ll = ([cpu], [dram], [dma])
    for i in range(3) {
      let x = lx.at(i)
      rect((x, 0.95), (x + 1.3, 1.7), stroke: 0.7pt, radius: 0.08)
      content((x + 0.65, 1.325), text(size: 8pt, ll.at(i)))
      line((x + 0.65, 1.7), (x + 0.65, 2.2), stroke: 0.7pt)
    }
    content((2.3, 0.35), text(size: 8pt, fill: c-dim)[一个物理互连承载全部成员])
    // 右:嵌套
    rect((6.2, 2.2), (11.4, 2.9), stroke: (dash: "dashed", thickness: 0.8pt), radius: 0.1)
    content((8.8, 2.55), [总线 `sys`(嵌套)])
    rect((6.4, 0.95), (7.7, 1.7), stroke: 0.7pt, radius: 0.08)
    content((7.05, 1.325), text(size: 8pt)[cpu])
    line((7.05, 1.7), (7.05, 2.2), stroke: 0.7pt)
    rect((8.0, 0.95), (9.3, 1.7), stroke: 0.7pt, radius: 0.08)
    content((8.65, 1.325), text(size: 8pt)[dram])
    line((8.65, 1.7), (8.65, 2.2), stroke: 0.7pt)
    rect((9.55, 0.1), (11.35, 1.7), stroke: (dash: "dashed", thickness: 0.7pt), radius: 0.1)
    content((10.45, 1.42), text(size: 7.5pt)[子总线 `periph`])
    content((10.45, 1.08), text(size: 7pt, fill: c-dim)[自带交叉开关])
    line((10.45, 1.7), (10.45, 2.2), stroke: 0.7pt)
    let sx = (9.68, 10.25, 10.82)
    let sl = ([uart], [spi], [gpio])
    for i in range(3) {
      let x = sx.at(i)
      rect((x, 0.28), (x + 0.5, 0.78), stroke: 0.6pt, radius: 0.05)
      content((x + 0.25, 0.53), text(size: 7pt, sl.at(i)))
    }
    content((8.8, 0.35), text(size: 8pt, fill: c-dim)[占一个节点])
  })
]

== 参数的走向:协商、清单、兑现 <sec-manifest>

附着建立的成员关系,首先被协商消费。总线在它的成员上运行双向参数协商——逐边结算的算法在 @ch-negotiation,本章只用它的形状:下行方向承载发起者一侧的供给,例如它占用的标识空间;上行方向承载响应者一侧的供给与要求,例如它服务的地址区域、它是否要求保序、它接受的最大传输尺寸。两股声明在总线上相遇,为每个成员结算出一个具体接口——端口上每一根线的形状——并结算出一张地址图与可达关系:哪个成员能到达哪些地址。

结算结果打包成一份#term[清单][manifest]:总线交给它的物理互连的那份数据,两者之间唯一的一次交接。清单由两部分构成。

其一,*成员表*,同质。每个成员一项,内容为:

- 方向:发起者还是响应者;
- 结算出的接口:该成员端口的线形状;
- 它的地址区域(响应者)或标识空间(发起者);
- 它的时钟域与电源域(协商得出,见下);
- 它落在哪个具名节点(@sec-placement)。

表中没有任何字段区分叶子设备与子总线——从物理互连的视角,二者无从区分,也无须区分:都是一个带接口、带地址、落在某个节点上的端口。

其二,*可达表*,由结算出的地址图导出:每个发起成员到达哪些地址区域、对应哪个成员。不属于本总线任何成员的地址,默认路由到朝父的那个成员——子总线的成员访问外部世界,走的就是这条默认路由。

成员表里的时钟域与电源域,和地址、接口一样是*协商结果*,不是就地贴的标签。时钟与电源本身也是协议(@ch-protocol):一个时钟域就是一条时钟总线——时钟源供给频率能力,用钟的成员把自己的时钟节点附着上去、协商出频率与域归属;电源域同理。一个设备因此同时是几条总线的成员:数据节点接数据总线,时钟节点接某条时钟总线,电源节点接某个电源域,各是一次独立的附着(@sec-attach)。这几张总线同在一张连接结构里、同一遍协商跑完,所以数据清单里的域字段与时钟、电源那侧的结算天然一致,不需要一道单独的对齐工序;数据物理互连拿这个字段,是为了在域边界安放跨时钟域同步器或电源隔离单元。时钟总线与电源总线同样各有自己的物理互连——时钟树、电源网格——所以"互连可替换"(@req-interconnect)对它们一并成立。

物理互连是清单与策略的函数。策略是 fabric 自己的构建期参数:选定哪种物理互连及其配置——交叉开关的仲裁方式、片上网络的拓扑与虚通道数、直连的成对方式。这个函数分两步,横跨两个阶段。第一步*能力校验*在协商期、清单装配之后立即运行(@ch-negotiation):这份清单,本 fabric 承载得了吗——端口数、标识宽度、地址图的形状是否超出实现的能力上限?承载不了就在协商期报错,指出清单中的哪一项超出哪条限制;绝不默默降级、默默截断——这类失败无须等到例化。第二步*产出硬件*在例化期执行:以已通过校验的清单例化互连本体,外加每个成员恰好一个端口,端口的线形状就是该成员在清单里结算出的接口,逐线一致。

清单是纯数据:方向、线形状、地址、域、节点名,没有闭包、没有对规格对象的引用。把物理互连当作生成器看,清单与策略合起来就是它的完整参数——参数进、模块出,与任何生成器无异(@sec-serialization-boundary):可以序列化存档,可以离开整机、单独喂给一个 fabric 做单元测试。

物理互连位于协商的下游:它消费已结算的结果,不重开协商。清单里的接口不是建议而是定论,fabric 不得为任何成员端口增删一根线、改动一位宽。

#不变量[凡会影响任何成员接口的事实,都是总线契约的条款,在协商中结算;物理互连不引入、也不修改任何出现在成员端口上的参数。]

这条不变量是可替换性的全部来源,值得把因果链讲透。fabric 之间真实存在会影响接口的差异:交叉开关天然保持同一对端点之间的事务顺序,自适应路由的片上网络则可能乱序送达;某种片上网络的内部流控可能希望事务标识更宽。若这些事实由 fabric 私自决定,成员的接口就成了 fabric 的函数——把交叉开关换成片上网络,某个响应者突然要应对乱序,某个发起者端口上的标识线多出两根:成员看到的硬件变了,"替换 fabric 不动成员"随即破产。因此本模型把一切此类事实上收为总线契约的条款,在协商中结算:事务保序与否,是成员在协商里声明、总线上结算的性质;标识宽度由协商从全体发起者的声明算出。fabric 拿到的清单已经含着这些条款,它的义务是兑现:片上网络若不天然保序,就配置确定性路由、或自带重排序缓冲;两样都做不到,就在能力校验一步拒绝这份清单。兑现的代价与手段封在 fabric 内部——它为内部流控多用几位标识也无妨,那些位不出现在任何成员端口上。于是同一份清单交给交叉开关与交给片上网络,每个成员看到的端口逐线相同;全部差异被关在互连本体之内。替换因此成立。

#图([参数的走向。成员声明双向相遇(蓝:下行;红:上行),在总线上结算;清单是唯一的交接;物理互连做能力校验后产出互连硬件与每成员一个端口(绿)。])[
  #syn-diagram(
    spacing: (10mm, 8mm),
    node((0, 0), [成员声明], name: <m>),
    node((1.5, 0), [协商 \ (在总线上)], name: <n>),
    node((3, 0), [清单 \ 成员表 + 可达表], name: <mf>, shape: fletcher.shapes.pill, fill: c-fill),
    node((4.6, 0), [物理互连 \ f(清单, 策略)], name: <f>),
    node((6.1, 0), [互连硬件 \ 每成员一端口], name: <o>),
    node((4.6, 1), [fabric 策略(构建期)], name: <p>, stroke: (dash: "dashed")),
    edge(<m>, <n>, "-|>", stroke: c-down, bend: 22deg, label: text(fill: c-down, size: 8pt)[下行:供给], label-side: left),
    edge(<m>, <n>, "-|>", stroke: c-up, bend: -22deg, label: text(fill: c-up, size: 8pt)[上行:供给与要求], label-side: right),
    edge(<n>, <mf>, "-|>"),
    edge(<mf>, <f>, "-|>"),
    edge(<p>, <f>, "-|>"),
    edge(<f>, <o>, "-|>", stroke: c-edge, label: text(fill: c-edge, size: 8pt)[结算接口,逐线一致], label-side: left),
  )
]

== 落到三阶段 <sec-ic-phases>

本章的机制沿三阶段流水线(@sec-triptych)各就各位:

- *构建。*构造每条总线并绑定 fabric 策略;书写附着行,把成员落在具名节点上。此时存在的只有意图:成员集合、落点映射、策略选择。没有硬件,也没有任何结算出的参数。
- *协商。*在每条总线的成员上做纯计算,结算接口与地址图,为每条总线装配一份清单,并就地对清单做 fabric 能力校验(@sec-manifest)——承载不了的清单在此报出。落点不参与计算,原样抄入清单(@sec-placement)。
- *例化。*每个物理互连消费自己的清单,产出硬件;子总线朝父的接口与父总线上该子总线所落节点的端口在此相接,这个接缝称为#term[缝][seam]——两侧各是一条总线协商的结果,不一致时缝上插入位宽/域适配器,一致时退化为直连。

一个直接推论:*物理互连可以各自独立例化。*协商整体先于例化,例化开始时每份清单都已完整;父总线的物理互连是它那份清单的纯函数,子总线的物理互连是另一份清单的纯函数,两个函数的输入互不引用对方的输出,例化因此没有先后依赖——可以分开执行、分开缓存,乃至分别交给不同团队。一个子系统能够脱离整机独立构建、独立测试(@req-ip),机制根源就在这里:它需要的一切都在自己那份清单里。

以一个最小而完整的两级设计走一遍全流程。顶层总线 `sys` 由片上网络承载;外设收进子总线 `periph`,由交叉开关承载:

```scala
val periph = Bus("periph", fabric = Crossbar())
periph.node("p_uart") <~ uart   // 响应者,服务 0x1000_0000 起 4 KiB
periph.node("p_spi")  <~ spi    // 响应者,服务 0x1000_1000 起 4 KiB
periph.node("p_gpio") <~ gpio   // 响应者,服务 0x1000_2000 起 4 KiB

val sys = Bus("sys", fabric = Noc(Mesh(2, 2)))
sys.node("n_cpu")    <~ cpu     // 发起者
sys.node("n_mem")    <~ dram    // 响应者,服务 0x8000_0000 起
sys.node("n_periph") <~ periph  // 子总线,作为一个成员
```

注释里的地址是各成员自己的声明,附着行只给落点;`sys` 的 fabric 策略把网格的三个路由器命名为 `n_cpu`、`n_mem`、`n_periph`,第四个路由器空置。

*协商。*`periph` 总线上,三个外设的地址区域结算出并集 `0x1000_0000`–`0x1000_2FFF`;`periph` 对父呈现这一并集与一个结算接口。`sys` 总线上,`cpu` 的可达关系结算为:`0x8000_0000` 起的区域到成员 `dram`;`0x1000_0000`–`0x1000_2FFF` 到成员 `periph`。产出两份清单。`sys` 的清单三项成员——表中 `periph` 一项与 `dram` 一项结构完全相同,都是"响应者 + 地址区域 + 接口 + 节点"。`periph` 的清单四项成员——`uart`、`spi`、`gpio` 各为响应者,外加朝父的成员一项,方向为发起者(外部访问由它注入),不属于三个外设的地址经它默认路由出去。

*例化。*`sys` 的片上网络从 `sys` 清单产出:2×2 网格,三个端口分别在 `n_cpu`、`n_mem`、`n_periph`,线形状取自各成员的结算接口。`periph` 的交叉开关从 `periph` 清单产出:四个端口。两次例化互不等待,先后皆可。最后接缝:`sys` 在 `n_periph` 的端口与 `periph` 朝父的端口相接。两侧接口各是两条总线协商的结果,不必逐位相同——例如 `sys` 侧结算为 64 位数据、主时钟域,`periph` 侧结算为 32 位、外设时钟域——不一致时,缝上插入一个位宽/域适配器;一致时,缝退化为直连。适配器两侧的接口都在协商期定案,它同样只是一个以固定参数例化的小生成器。

#图([例题。上:`sys` 的片上网络,三个具名节点在用;下:`periph` 的交叉开关。绿色是缝:两侧结算接口不一致时插入位宽/域适配器。])[
  #syn-canvas({
    import cetz.draw: *
    // cpu / dram 成员
    rect((1.0, 6.6), (3.0, 7.35), stroke: 0.7pt, radius: 0.08)
    content((2.0, 6.975), text(size: 8pt)[cpu(发起者)])
    rect((7.2, 6.6), (9.6, 7.35), stroke: 0.7pt, radius: 0.08)
    content((8.4, 6.975), text(size: 8pt)[dram:`0x8000_0000` 起])
    line((2.0, 6.6), (2.0, 6.18), mark: (end: ">"), stroke: 0.8pt)
    line((8.4, 6.6), (8.4, 6.18), mark: (end: ">"), stroke: 0.8pt)
    // sys 的物理互连
    rect((0, 4.2), (10.4, 6.0), stroke: 1pt, radius: 0.1)
    content((5.2, 5.1), [`sys` 的物理互连:2×2 网格片上网络(一路由器空置)])
    circle((2.0, 6.0), radius: 0.13, fill: black)
    content((2.0, 5.62), text(size: 8pt)[`n_cpu`])
    circle((8.4, 6.0), radius: 0.13, fill: black)
    content((8.4, 5.62), text(size: 8pt)[`n_mem`])
    circle((5.2, 4.2), radius: 0.13, fill: black)
    content((5.2, 4.56), text(size: 8pt)[`n_periph`])
    // 缝与适配器
    line((5.2, 4.2), (5.2, 3.62), stroke: 1pt + c-edge)
    rect((4.55, 3.02), (5.85, 3.62), stroke: 0.8pt + c-edge, radius: 0.08)
    content((5.2, 3.32), text(size: 8pt)[适配器])
    content((7.7, 3.32), text(size: 8pt, fill: c-dim)[缝:64→32 位 · 域穿越])
    line((5.2, 3.02), (5.2, 2.44), stroke: 1pt + c-edge)
    // periph 的物理互连
    rect((1.6, 0.5), (8.8, 2.3), stroke: 1pt, radius: 0.1)
    content((5.2, 1.4), [`periph` 的物理互连:交叉开关])
    circle((5.2, 2.3), radius: 0.13, fill: black)
    content((5.2, 1.92), text(size: 8pt)[朝父端口])
    let bx = (2.6, 5.2, 7.8)
    let bl = ([uart \ `0x1000_0000`], [spi \ `0x1000_1000`], [gpio \ `0x1000_2000`])
    for i in range(3) {
      let x = bx.at(i)
      circle((x, 0.5), radius: 0.13, fill: black)
      rect((x - 1.05, -1.5), (x + 1.05, -0.55), stroke: 0.7pt, radius: 0.08)
      content((x, -1.025), text(size: 8pt, bl.at(i)))
      line((x, -0.55), (x, 0.32), mark: (end: ">"), stroke: 0.8pt)
    }
  })
]

至此机制闭合。逻辑结构——成员、可达、地址、域——在总线上描述一次,在总线上协商一次;清单是那唯一而完整的一次交接;物理互连的职责止于兑现清单。更换互连,改的是构造总线时的 fabric 策略,以及随新拓扑重定的节点名(@sec-placement);总线承载的逻辑事实与协商结果一个字不动。谁到达谁属于总线,如何到达属于物理互连——#ref(<req-interconnect>)要求的分离,由这条单向的数据流兑现。
