#import "../lib.typ": *

= 硬件边界 <ch-hardware>

前六章都在协商这一侧。本章规定协商与硬件生成之间的接口：生成器模块如何声明它与 zaozi 生成器的关系、节点如何落到真实的接口字段上、例化期按什么次序把一切装配起来。

== 生成器的契约 <sec-generator-contract>

Syntheke 对硬件构造只有一个假设：存在#term[生成器][generator]——即以一份参数为唯一输入、以一个电路模块为输出的工厂函数。这个假设展开为四条，每条都有其存在的理由：

+ 消费*一个可序列化参数*，产出一个电路模块——参数必须能写入文件，协商结果才能归档、传输到其他机器、事后原样重放（@sec-serialization-boundary）。
+ 模块的硬件接口、探针接口、层结构都*只看参数就能算出*——协商期要在不生成任何电路的前提下规划端口（@sec-punch-planning）、核对绑定（@sec-generator-module）；如果接口形状要等电路造完才知道，这两件事都无从谈起。
+ 同一参数必然产出同一模块——去重与缓存（@sec-dedup）的全部依据就这一条：参数相等即模块相等，不需要比对电路。
+ 生成器可以脱离任何框架独立运行（命令行两步：先把参数写成 JSON 文件，再按这份文件生成电路）——生成器不知道 Syntheke 存在，Syntheke 只是它众多调用者之一；两者之间的全部往来就是那一份参数，再无其他接口。

zaozi 的生成器恰好满足这四条。

#图([序列化边界的两侧。生成器模块持有用户参数与两个纯函数（合并、协议参数推导）；生成器持有对参数的四个函数。二者之间只有完整参数往来。])[
  #syn-canvas({
    import cetz.draw: *
    rect((0, 0), (5.0, 3.4), stroke: 0.7pt, radius: 0.1)
    content((2.5, 3.05), [*生成器模块*（协商域）])
    for (y, t) in ((2.35, [用户参数（构建期写定）]), (1.65, [`combine : (UP, PP) => FP`]), (0.95, [`computeProtocolParam`]), (0.25, [节点声明 + 硬件绑定])) {
      content((2.5, y), text(size: 8.5pt, t))
    }
    rect((7.6, 0), (12.4, 3.4), stroke: 0.7pt, radius: 0.1)
    content((10.0, 3.05), [*生成器*（硬件域）])
    for (y, t) in ((2.35, [硬件接口 $=f("参数")$]), (1.65, [探针接口 $=f("参数")$]), (0.95, [层结构 $=f("参数")$]), (0.25, [电路体 $=f("参数")$])) {
      content((10.0, y), text(size: 8.5pt, t))
    }
    line((6.3, -0.3), (6.3, 3.7), stroke: 2.2pt)
    line((5.15, 1.7), (7.45, 1.7), mark: (end: ">"), stroke: 1.1pt + c-edge)
    content((6.3, 2.12), text(fill: c-edge)[完整参数])
    content((6.3, 3.95), [序列化边界])
  })
]

== 生成器模块的声明 <sec-generator-module>

生成器模块（@sec-module-kinds）在构建期声明五件事，缺一不可：

```scala
abstract class GeneratorModule[UP, PP, FP, I](
  val generator: Generator[FP, ?, I, ?],   // 恰好一个生成器
  val userParam: UP,                       // 用户参数
  val combine:   (UP, PP) => FP,           // 双层合并
):
  def computeProtocolParam(view: EdgeView): PP   // 协商结果的推导
  // 节点声明必须携带硬件绑定：
  //   sinkNode(protocol, terms, hwBinding = io => io.someField)
```

#term[硬件绑定][hardware binding]建立节点与生成器接口字段之间的对应：一个从例化后的接口引用映射到具体字段引用的函数。它在构建期只是被记录的闭包；例化期生成器实例化之后，框架应用它得到该节点每条边的硬件端点，供连线计划引用。绑定必填——生成器接口有多个同协议字段时，任何默认绑定的推断都会引入难以定位的错误。

#决策([绑定校验在例化期做结构比对])[
  绑定字段的硬件类型必须与该节点边参数的 `interfaceOf(edge)` 结构一致（字段名、宽度、翻转逐层相同）。校验时机选在例化期、方式选结构比对，而非把协议边参数与硬件类型在类型系统层面绑定：后者使每个协议库与每个生成器库强耦合，任何一侧演化都牵动另一侧的类型签名。失配报错携带节点身份、连接位置、期望与实际形状的差异路径。
] <dec-binding-check>

#开放([类型级绑定强化])[
  在结构校验之上，可以为愿意付出耦合代价的协议库提供可选的类型类见证（协议 `Edge` 与硬件接口字段类型的对应关系），把一部分失配提前到编译期。属于协议库的可选增强，不进入内核契约。
] <open-typed-binding>

== 例化流程 <sec-elaboration-flow>

例化期对层次树自底向上执行，每个模块一步：

+ *生成器模块*：取协商期算好的协议参数，合并 $"FP" = "combine"("UP", "PP")$；调用生成器（其缓存保证同参不重复生成）；应用各节点的硬件绑定，登记每条边的硬件端点。
+ *结构模块*：按#ref(<ch-hierarchy>)清单发射端口、子实例与连线。连线是 bundle 级整体连接，由 zaozi 的连接算子处理字段方向——Syntheke 不逐字段枚举。

次序保证引用先于使用：连到子实例端口时，子模块必已发射。整个例化期没有一次决策——它只是协商结果的忠实展开，任何在此阶段才暴露的问题（绑定失配除外）都应视为协商期的缺陷，并在协商期修正。

== 序列化清单 <sec-serialization-list>

#table(
  columns: (auto, 1fr, 1fr),
  table.header([类别], [内容], [约束]),
  [*必须可序列化*], [完整参数（用户参数 $+$ 协议参数合并后）。], [进入生成器的唯一对象；zaozi 以之为缓存键与命令行输入（R8）。],
  [*可选可序列化*], [拓扑、边参数、端口与连线计划、层树。], [供 dump、可视化、审计（@ch-tooling）；格式演进不构成兼容性契约。],
  [*从不序列化*], [参数变换函数、硬件绑定、合成与推导函数——一切闭包。], [闭包活在单次进程内。硬要写进文件，只能把代码存成文本、加载时再求值——这构成宏系统而非参数，复现性与可审计性同时丧失。],
)

协商与硬件的接口至此讲完。还剩一类同样要跨越模块层次的信号刻意推迟到现在——为验证而生的探针，下一章。
