#import "../lib.typ": *

= 层次穿越与模块生成 <ch-hierarchy>

协商图上的一条边，两端可能位于层次树上相距任意远的两个模块（R6）。要把这条逻辑边变成电路，必须在沿途每个模块边界开出端口、逐层连线。本章规定这套#term[打洞][port punching]机制：端口在哪里出现、叫什么名字、结构模块最终发射什么。这里的每条规则都服务于同一个目标——*确定性*：同一规格，字节级相同的产物。

== 打洞规划 <sec-punch-planning>

对每条已结算的边 $e$，设两端节点分别位于模块 $A$ 与 $B$，$W = "LCA"(A, B)$ 为二者在层次树上的最近公共祖先。规划规则：

+ 从 $A$（含）向上到 $W$（不含）的路径上，每个模块的边界追加一个*出向*端口；从 $B$ 向上到 $W$ 对称地追加*入向*端口。
+ 连线计划随之生成：$A$ 内部从节点到本层端口一条连线；路径上每层，子实例端口与本层端口一条连线；在 $W$ 内部，两侧下来的端口（或位于 $W$ 本层的节点）之间一条连线。
+ 特例退化自然成立：两端同模块（$A = B = W$）时无端口，只有一条模块内连线；一端是另一端的祖先时，只有一侧打洞。

端口的硬件形状取自协议接口 `interfaceOf(e)`（@sec-protocol-interface）翻译成的 FIRRTL 类型；端口的宏观方向由边的方向给出（沿边为出），接口内部字段的翻转（如握手信号的反向就绪线）由协议接口中的 flip 标记保留。

#图([打洞。节点 a、b 分居两棵子树，LCA 为顶层。M1 边界出现出向端口，M2 边界出现入向端口，三段连线分属三个模块。])[
  #syn-diagram(
    spacing: (16mm, 8mm),
    node((0, 0), [节点 a], name: <a>, shape: fletcher.shapes.circle),
    node((1, 0), [▸], name: <p1>, inset: 2.5pt, fill: rgb("#fdf3d7")),
    node((2, 0), [▸], name: <p2>, inset: 2.5pt, fill: rgb("#fdf3d7")),
    node((3, 0), [节点 b], name: <b>, shape: fletcher.shapes.circle),
    node(enclose: (<a>, <p1>), stroke: c-hier, inset: 11pt, snap: false, name: <m1>),
    node(enclose: (<p2>, <b>), stroke: c-hier, inset: 11pt, snap: false, name: <m2>),
    node(enclose: (<m1>, <m2>), stroke: c-hier, inset: 24pt, snap: false),
    node((0, -0.72), text(size: 8pt, fill: c-hier)[模块 M1], stroke: none),
    node((3, -0.72), text(size: 8pt, fill: c-hier)[模块 M2], stroke: none),
    node((1.5, -1.28), text(size: 8pt, fill: c-hier)[顶层（LCA）], stroke: none),
    edge(<a>, <p1>, "-|>", stroke: c-edge, label: text(size: 8pt)[M1 内连线], label-side: left),
    edge(<p1>, <p2>, "-|>", stroke: c-edge, label: text(size: 8pt)[顶层连线], label-side: left),
    edge(<p2>, <b>, "-|>", stroke: c-edge, label: text(size: 8pt)[M2 内连线], label-side: left),
  )
]

打洞是*规划*而非动作：它发生在协商期，产物是纯数据的端口清单与连线清单，挂在协商结果上；例化期只是按清单发射。于是"某个模块为什么多了这个端口"永远可以在协商产物里追查到那条边与它的书写位置。

== 端口命名 <sec-port-naming>

端口名必须同时满足三件事：在所属模块内唯一；从名字能读出来历；*规格的局部变更只造成名字的局部变更*。规则递归给出：

- *节点侧基名。*位于模块 $A$ 的节点在本层产生的端口，名为 `{节点名}_{in|out}_{k}`，其中节点名取声明时的宿主语言变量名，$k$ 是该边在节点端口向量中的索引（@sec-star-solving）。
- *逐级上传。*模块 $w$ 中对应子实例 $c$ 内部端口 $p$ 的端口，名为 `{c 的实例名}_{p 的名字}`。

例如：模块 `l2` 内节点 `mem` 的第 2 条出边，在 `l2` 边界得名 `mem_out_2`；`l2` 在 `soc` 中的实例名为 `l2`，则 `soc` 边界的端口名为 `l2_mem_out_2`。

唯一性由构造保证：同一模块内实例名互异、节点变量名互异、索引互异，三段拼接不可能碰撞，*因此不存在任何"重名消解"步骤*——重名消解（自动追加序号）与确定性目标直接冲突：一个无关端口的加入会使全部同名端口的序号重排。

#决策([端口名不含哈希，接受线性长名])[
  端口名只由实例路径、节点名与端口索引拼成，不掺入任何内容哈希。名字长度随层深线性增长——接受：可读性与稳定性优先，且工具（@ch-tooling）能把名字反解回节点与边。变更局部性承诺：增删一条连接，只影响同一节点上声明序在其后的连接的索引段；增删无关的子实例或节点，不影响任何既有端口名。
] <dec-port-naming>

== 结构模块的发射 <sec-wrapper-emission>

结构模块（@sec-module-kinds）的电路内容由框架整体生成，清单封闭：

+ *端口*：打洞规划产生的全部端口，按协议接口翻译；
+ *子实例*：每个子模块一条实例语句；
+ *连线*：连线计划中属于本层的全部连接，bundle 级整体连接；
+ *层声明*：子树探针所需的层集合（@ch-verification）。

#不变量[结构模块的电路体不含上述四类之外的任何语句——没有运算、没有状态、没有条件。例化期可以（并且应当）在输出上机械检查这条性质。]

恒等节点在此化为一条本层连线；瞬态节点早在协商期已被消除（@sec-passes），发射器根本不知道它存在过。

== 模块身份与去重 <sec-dedup>

同样的子系统在设计中出现多次——四个相同的处理器簇、两份相同的链路控制器——发射四份相同的 FIRRTL 模块是浪费；交给下游综合工具去重，则去重结果取决于工具实现，不再确定。Syntheke 在发射前按#term[结构键][structural key]去重：

- 生成器模块的键 $=$（生成器身份，完整参数）。这正是 zaozi 的按参数缓存（@sec-two-layer-params）：完整参数含协议参数，两个"看似一样"但协商环境不同的实例自然分键。
- 结构模块的键 $=$ 规范化的（端口形状序列，各子实例的模块键，连线集合，层声明）——自底向上递归可算。

键相同的模块只发射一次，其余实例引用之。模块的*名字*与键解耦：取构建期的类型名或显式命名，同名而不同键时按首次出现顺序加确定性序号。哈希只出现在去重键里，不出现在任何名字里——名字面向人类阅读，键仅用于机器比对。

#开放([跨设计的增量缓存])[
  结构键天然适合做跨次构建的缓存索引（键未变则跳过发射乃至下游编译）。这需要键的序列化格式承诺稳定，涉及版本化策略，v1 不承诺，留待工具链成熟。
] <open-incr-cache>

打洞、命名、发射、去重——四步都只是对协商结果的机械展开。端口、连线、模块名至此全部确定；硬件逻辑如何接入，见下一章。
